# HEARTBEAT — 项目心跳记录

> 按时间顺序记录工作流水：完成了什么、当前卡在哪、下一步做什么。
> 规则：每次 commit 前必须追加一条记录；阻塞时写 BLOCKED。

---

## 2026-08-03 09:20 — 项目启动

**完成：**
- 创建项目骨架目录、git 仓库（develop 分支，main 待首次提交后建立）
- 编写 MEMORY.md（架构决策 + 数据规范速查）

**下一步：**
- [x] 服务器脚手架：package.json、server.js、index.html、style.css
- [x] 数据层三件套：equipment.js → ships.js → maps/expeditions/quests
- [x] state.js 存档系统
- [x] battle.js 战斗引擎
- [x] generate_art.js SVG 立绘生成
- [x] UI 各屏幕
- [x] simulate.js headless 验证

---

## 2026-08-03 17:30 — v0.1 完成（全功能可玩）

**完成：**
- 数据层：44件装备 / 55艘美舰（含改/改二形态）/ 3海域 / 8远征 / 日常8+周常6+月常2+一次性14任务
- 核心引擎（battle.js）：制空权5档+S1/S2+空袭+先制对潜+开幕雷击(SS)+交战形态+昼战炮击(射程序/BB二轮/观测射击CI/连击)+昼战雷击+夜战(鱼雷CI/主鱼CI/连击)+对空CI+经验/掉落/血条/疲劳/补给惩罚
- 核心引擎（其余）：建造(配方池+稀有度加权)/开发(秘书舰池)/远征(条件+大破失败)/入渠/补给/等级/改造/近代化/任务追踪/演习
- 美术：57张 SVG 立绘（舰娘55 + 深海通用2）
- UI：母港/编成/舰娘详情(装备/改造/近代化/解体)/出击(海域地图+路线分歧+阵型选择+战斗演出)/工厂/后勤(远征/入渠/补给/演习)/任务
- 测试：`npm run sim` 255项引擎断言全过；`test_flow.html` 浏览器端 39项 E2E 全过
- 修复记录：dealDamage 传参bug / STAT_NAMES 未导出 / 经验曲线过陡(改平坦) / 演习敌方用真实舰船 / finishTimers 误删建造队列 / window.Game 未挂载 / esc 未定义 / BOSS 平衡(满级舰队50战全胜，正常)

**下一步（后续迭代候选）：**
- [ ] 活动海域（贴条/削甲/友军/多段血条）
- [ ] 图鉴收集率、更多海域（1-4~1-6）
- [ ] 音乐/音效、战斗动画增强
- [ ] 浏览器 E2E 脚本化（自动化运行 test_flow）

---

## 2026-08-04 — v0.2 强化与改修工厂（参照 kcwiki 近代化改修 + 明石的改修工厂）

**完成：**
- 近代化改修按 wiki 重做（progression.js）：
  - 素材属性值表（舰种/改造形态决定，参照 wiki 素材列表：DD 火力1雷装1、BB 火力3装甲3…）
  - 多素材合成（最多5艘）+ 预览（素材合计/奖励点/显示上升/偏斜值/剩余上限）
  - 奖励/偏斜公式（奖励值=(n+1)÷5+n、偏斜=((n+2)÷5+n)÷2，+4/+9/+14/+19/+24 奖励点）
  - 属性上限：火力/雷装/对空/装甲=基础×1.3；海防舰(DE)改修：耐久+2/对潜+9/运+8（wiki：对潜/耐久/运改造后继承）
  - 普通素材不再能喂 回避/索敌（wiki：只有火力/雷装/对空/装甲）
  - 改造后火力/雷装/对空/装甲改修值重置（wiki 规则，UI 有警告）
- 装备改修工厂（新增 game/improve.js + 工厂页签，明石=维斯塔尔工作舰）：
  - 维斯塔尔秘书舰解锁；二号舰决定可改修类别（basic/dd/cl/bb/cv/as，简化 wiki 星期×二号舰）
  - 每日次数 1~3（二号舰工作舰/水母+1，维斯塔尔改+1）；每日重置
  - ★0→10，★+4 前必成功，之后按 wiki 成功率表（明石 vs 明石改 两套）
  - 确定化（双倍螺丝必成功）；★+6 起消耗同名★0素材；★MAX 可更新进化（新装备★5 起步，23 条更新路线）
  - 消耗改修资材(螺丝) + 资源；螺丝上限 3000，日常任务「装备的改修强化」+1/天
  - 改修效果=类别系数×√★（大主炮1.5/鱼雷1.2/其余1.0；舰战舰攻★×0.2；电探水侦→索敌；声呐爆雷→对潜）
- 任务：日常 d9（改修1次→1螺丝）、周常 w7（改修5次→3螺丝）、月常 m3（改修10次→5螺丝）、一次性 o15（获得维斯塔尔→10螺丝）
- 存档兼容：旧档自动迁移（screws/improve/equipment.star 默认值）
- 测试：`npm run sim` 328 项全过（新增 73 项）；浏览器 E2E 45 项全过（新增改修工厂/近代化冒烟）

**下一步（后续迭代候选）：**
- [ ] 活动海域（贴条/削甲/友军/多段血条）
- [ ] 图鉴收集率、更多海域（1-4~1-6）
- [ ] 音乐/音效、战斗动画增强
- [ ] 浏览器 E2E 脚本化（自动化运行 test_flow）

---

## 2026-08-04 17:20 — 母港 UI 重构（参照 UI 设计分析报告）+ 近代化改修入口显性化

**问题：** 近代化改修入口只藏在舰娘详情模态框里，主页看不到；主页布局为简单双面板，信息层级弱。

**完成：**
- 母港按报告「三段式」重构：左侧功能菜单（出击橙色主按钮/编成/工厂/入渠/补给/远征/任务/演习，图标+文字）+ 中央主显示区 + 状态栏
- 新增秘书舰面板：旗舰大立绘 + 舰名/台词 + 快捷操作（出击/编成/近代化改修）
- 第一舰队改为 6 列网格，每张舰娘卡片下带快捷操作：补给 + 近代化改修（可改修时显示）
- 近代化改修入口 3 处：主页卡片快捷按钮 / 秘书舰面板按钮 / 舰娘详情模态框（原入口保留）
- 立绘真实化：portraitImg 改为加载 `art/portraits/{id}.svg`（55 张全存在），缺失时回退舰名文字
- 状态面板补充：远征(含第一舰队)/入渠/改修资材/可领取任务；响应式（≤900px 菜单横排、网格3列）
- 验证：headless 布局检查（无横向溢出、菜单148px、6列网格不换行、7个改修入口、立绘文件齐备）；`npm run sim` 328 项全过；E2E 45 项全过

**下一步（后续迭代候选）：**
- [ ] 活动海域（贴条/削甲/友军/多段血条）
- [ ] 图鉴收集率、更多海域（1-4~1-6）
- [ ] 音乐/音效、战斗动画增强
- [ ] 浏览器 E2E 脚本化（自动化运行 test_flow）

---

## 2026-08-04 18:10 — 顶部一级菜单置顶UI + 近代化改修迁入工厂二级菜单

- 废弃主页左侧「司令室」侧栏与旧顶部导航，改为顶部一级菜单置顶UI（`ui/common.js` refreshNav）：🏠母港/⚔️出击/👥编成/🛠工厂/🔧入渠/⛽补给/🚢远征/🏆演习/📋任务，图标+文字、active 金色高亮、悬停放大+阴影；入渠/补给/远征/演习为 logistics 各 tab（nav 支持 tab 参数与 active 匹配）
- 近代化改修统一迁入「工厂 → 近代化改修」二级菜单（`ui/factory.js` modernizePanel）：列出全部舰船（等级/舰种/剩余可改修属性/改修MAX），点击弹素材选择（复用 `Homeport.openModernize`，已导出）；主页卡片与秘书舰面板的改修入口移除，舰娘详情弹窗改为文字引导「工厂 → 近代化改修」
- 立绘保持舰种占位符（上轮修改）；主页 CSS 清理侧栏样式
- 测试：`npm run sim` 328 项全过；E2E 48 项全过（新增 nav:topmenu / screen:factory-modernize / modernize:modalFromFactory）

**下一步（后续迭代候选）：**
- [ ] 活动海域（贴条/削甲/友军/多段血条）
- [ ] 图鉴收集率、更多海域（1-4~1-6）
- [ ] 音乐/音效、战斗动画增强
- [ ] 浏览器 E2E 脚本化（自动化运行 test_flow）

---

## 2026-08-05 — 装备系统 v2（参照 kcwiki「开发」「开发资材」「装备」）

**问题：** 装备系统此前为简化版（20秒开发队列、仅资源消耗、无装备解体/仓库），与 wiki 差异大。

**完成：**
- 开发资材（新资源 devMats，上限3000）：开发成功消耗1、失败返还（wiki 原文）；获取：日常 d6「装备开发3次」+1、周常 w5 +3、月常 m1 +5 / m2 +8、一次性 o16「狙击开发」+5、远征 ex7 +1 / ex8 +2；新档初始10
- 开发系统按 wiki 重做（factory.js）：秘书舰系 炮战GUN/水雷MINE/空母CV/潜水SUB × 最高资源池 油钢/弹药/铝（优先级 燃料钢材>弹药>铝）= 9 开发池；每池50等份、出货率=份额×2%、失败份额=50-Σ；roll 出装备后判定 提督等级≥稀有度×3 且 四项资源≥解体回收值×10，否则失败；开发即时结算（取消20秒队列），成败均计入日常任务
- 开发 UI：实时预览当前配方所在开发池（装备列表/出货率/失败率/最低资源要求），8 个 wiki 常用预设配方；开发结果模态框
- 装备解体 + 装备仓库页签（ui/factory.js equipPanel）：按类别分组一览（★/锁定/装备中状态），解体回收资源；装备中/上锁不可解体
- 装备数据 44→50 件：新增 5inch连装炮 Mk.28 mod.2、5inch单装炮 Mk.30、FM-2、F6F-5、GFCS Mk.37、SG雷达(初期型)；全部装备补齐 r(稀有度)/scrap(解体回收)/dev(开发条目) 三字段，IMPROVE 改修表同步扩充（含 Mk30→Mk28、FM-2→F6F-5 等更新路线）
- 存档迁移：旧档补 devMats=10、废弃旧 development 队列；顶栏新增开发资材紫色图标；母港状态面板显示开发资材
- 测试：`npm run sim` 389 项全过（新增开发池判定/狙击公式/资材返还/解体/远征资材 31 项）；E2E `test_flow.html` 48 项全过（新增开发面板/仓库/解体冒烟）

---

## 2026-08-05 18:40 — 修复：母港舰娘详情弹窗崩溃（装备槽数据格式不符）

**问题：** 母港点击舰艇图标无法打开详情（属性/立绘/装备栏）。E2E 此前只断言屏幕渲染长度，未覆盖点击交互，漏检。

**根因：** `ships.js` 的 `slots` 为槽位类型数字数组（如 `[1,13]`），而 `homeport.js` 详情弹窗与装备选择器按 `{types:[...]}` 对象取（`sl.types.map` / `slot.types.includes`）→ `Cannot read properties of undefined (reading 'map')`。属 v0.1 遗留 bug（archive 版同错）。

**修复：**
- `homeport.js` 两处改为兼容 `(sl.types || sl)`（数字数组直接使用）
- E2E 新增「shipdetail」回归测试：点击舰艇卡片 → 详情弹窗（属性/装备槽）→ 点击槽位 → 装备选择器 → 可选装备列表

**验证：** `npm run sim` 389 项全过；E2E 57 项全过（新增 shipdetail 4 项，`errors: []`）。

---

## 2026-08-05 19:20 — 装备开发 10 连（批量开发）

**完成：**
- `game/factory.js` 新增 `developBatch(recipe, secretaryUid, count)`：资源按份数整批校验（不足整批拒绝），逐次开发；开发资材按成功数逐个消耗，不足时提前停止（`stopped` 标记与次数）
- 开发面板新增「10连开发 ×10」按钮：结果弹窗汇总 成功/失败/消耗资材 + 获得装备清单（同名合并 ×N），资材不足时提示提前停止
- 批量开发同样计入每日开发任务与开发统计（每次单独计数）
- 测试：`npm run sim` 421 项全过（新增批量开发 7 项：全成功/资材×10/资源×10/入仓/资材不足停止/停止后资材归零/资源不足拒绝）；E2E 61 项全过（新增 dev10 按钮与结果弹窗冒烟）

---

## 2026-08-05 20:10 — 装备装卸交互优化：详情页常驻 + 选择器分组单列

**问题：** 装备选择器（UI.modal）会覆盖并关闭舰艇详情弹窗，换完一件装备后必须重新点击舰艇图标进入详情页，无法连续换装；选择器装备列表为横向小卡片，查找不便。

**完成：**
- 新增次级浮窗 `UI.subModal`（`#sub-modal-root`，z-index 150 叠于主弹窗之上）：关闭只清除本浮窗，不影响下层舰艇详情页
- 舰艇详情弹窗重构为可刷新（detailHtml/wire/refresh 分离）：装备选择器以次级浮窗打开，替换/卸下/取消后仍停留在详情页，且详情页即时刷新显示新装备与属性
- 装备选择器按装备种类分组单列（`eq-cat-title` + `eq-pick-item`，固定类别顺序），显示装备名称/★/属性；顶部展示当前槽位装备与可装槽位类型
- E2E 增强：验证 选择器打开时详情页保持、替换后选择器关闭且详情页仍打开、装备已变更
- 测试：`npm run sim` 421 项全过；E2E 65 项全过（shipdetail 扩充 8 项）

---

## 2026-08-05 20:30 — 修复：装备更换后详情页 × 无法关闭

**问题：** 详情页在装备更换后保持打开（上轮优化），但主动点击 × 关不掉。原因：`UI.modal` 只在创建时给 `[data-close]` 绑一次关闭监听，而详情页 `refresh()` 重建 DOM 后新 × 元素没有监听器。

**修复：** `openShipDetail` 的 `wire()`（refresh 后重绑）中重新绑定 `[data-close] → m.close()`。

**验证：** E2E 新增 `shipdetail:detailClosedByX`（替换装备后点 × 关闭详情页返回主页）；`npm run sim` 421 项全过；E2E 66 项全过，`errors: []`。

**下一步（后续迭代候选）：**
- [ ] 活动海域（贴条/削甲/友军/多段血条）
- [ ] 图鉴收集率、更多海域（1-4~1-6）
- [ ] 音乐/音效、战斗动画增强
- [ ] 浏览器 E2E 脚本化（自动化运行 test_flow）

---

## 2026-08-06 — 弹药后勤 v2（参照 kcwiki「弹药补正」「1-2」）

**问题：** 玩家在 1-2 第三战（BOSS）时全队「弹药耗尽，无法攻击」——反复出击不补给导致弹药归零，新手图 1-2 打不通。

**完成：**
- **1-2 按 wiki 重构**（`data/maps.js`）：
  - C 点由战斗点改为**弹药资源点**（wiki：1-2 是第一个有资源点的海域，通过不消耗油弹及士气）
  - 分支翻转：索敌≥20 发现弹药补给路线 C→D（一战到 BOSS），否则走 A→B 长路线
  - BOSS 舰队 F06 按 wiki 重做：敌方只有驱逐/轻巡/雷巡、≤5 艘（wiki：1-2 不会出现重巡以上），新增深海重雷装巡洋舰CHI级(CLT) 旗舰，替换原 85HP 深海重巡栖姬+双精锐的过度配置
- **弹药补正按 wiki 修正**（`game/battle.js`）：残弹率≥50%→1，<50%→残弹率/50（0% 无法炮击），由「阈值前乘算」改为 **阈值后补正**作用于最终伤害（wiki：造成伤害=(最终攻击力-防御力)×弹药补正）；雷击战/夜战/开幕雷击/先制对潜同步修正；弹药按战斗开始时残弹计算（wiki）
- **补给引导**：出击页显示舰队油弹% + 一键补给按钮 + 低油弹（<50%）出击确认警告（`Sortie.start` 返回 warn）；战斗后弹药耗尽追加补给提示；出征中节点页显示油弹%；补给页文案修正（原「<25%无法炮击」错误）
- 测试：`npm run sim` 443 项全过（新增弹药后勤 22 项：补正公式/1-2 资源点与 BOSS 构成/C→D 路线满补给至 BOSS 弹药≥50%/低油弹警告/0% 弹药战斗）

---

## 2026-08-06 — 海域扩充（1-4 / 2-X / 3-X，参照 kcwiki「出击」）

**问题：** 原仅 3 张海域（1-1~1-3），内容偏少，缺少难度梯度纵深。

**完成：**
- 新增 9 张海域，共 12 张（`data/maps.js`），星级 3★→10★ 平滑递增：
  - **镇守府海域**：1-4 近海防卫线（wiki 1-4 南西群岛防卫线，驱逐栖姬 BOSS）
  - **南西群岛海域**：2-1 图拉吉急袭（重巡flagship）、2-2 铁底湾海峡（wiki 2-2 巴士岛近海，潜水栖姬 BOSS，反潜装备检查点）、2-3 圣克鲁斯海域（wiki 2-3 东部奥廖尔海：5战长航路+燃料/弹药资源点+输送舰队沟点+战列舰Ru级flagship BOSS，制空≈wiki 空确162）、2-4 瓜达尔卡纳尔攻略（wiki 2-4 冲之岛海域，飞行场栖姬 BOSS）
  - **北方海域**：3-1 北大平洋哨戒（wiki 3-1 莫雷海，通商破坏+Ru级flagship×2 BOSS）、3-2 基斯卡岛近海（wiki 3-2 基斯岛近海，驱逐舰≥5 带路走弹药捷径，包围舰队=轻巡flagship+输送舰）、3-3 阿图岛方向（wiki 3-3，Ta级flagship×2 BOSS）、3-4 北方海域全域（wiki 3-4，北方栖姬 BOSS 170HP/78甲/84炮，当前最终海域）
- 新深海模板（wiki 二期命名）：驱逐III级/轻巡To级/重巡Ne级/战列舰Ru级/轻空母Nu级/潜水Ka级/输送舰Wa级(AP)/雷巡CHI级精锐 + 栖姬 潜水/飞行场/北方；输送舰无武装作击破目标
- 带路系统扩展：`branch` 支持数组多分歧点（3-1 索敌+驱逐双带路、3-2 双驱逐带路），UI 显示最大索敌要求
- 任务链：周常 w8(2-3)/w9(3-2)、月常 m4(3-4)、一次性 o17~o25（通关链+潜水/飞行场栖姬 S 胜讨伐，送西弗吉尼亚/北卡罗来纳/萨拉托加）
- 难度梯度实测调校（全流程出击探针）：阶段W（改1 Lv30）2-2 无反潜装 S 胜 0%→带声呐爆雷 80%；阶段A/B/C（改1 Lv40 → 改二 Lv70 → 满级）S 胜率逐级爬升；北方栖姬按实测调至 84炮/78甲
- 测试：`npm run sim` 831 项全过（新增 42 项：数据校验+难度梯度+冒烟）；E2E 64 项全过

**下一步（后续迭代候选）：**
- [ ] 活动海域（贴条/削甲/友军/多段血条）
- [ ] 图鉴收集率、更多海域（EO 图 1-5/2-5/3-5）
- [ ] 音乐/音效、战斗动画增强

---

## 2026-08-06 — 出击UI重构（Kancolle世界地图风格海域选择）

**问题：** 海域选择为单列长卡片罗列，12 张图后既不美观也不高效。

**完成：**
- 三段式布局（`ui/sortie.js` + `style.css`）：
  - 海域导航页签：[1 夏威夷海域] [2 所罗门群岛海域] [3 阿留申群岛海域]，金色高亮切换（后经命名修订扩展至 5 区域）
  - 海图面板：深蓝海面+经纬网格+罗盘+区域标题；各海域地图以编号圆标地理化排布（已攻略=绿底✓），虚线航路串联，点击切换选中（金色光晕）
  - 地图详情面板：迷你海图预览 + 名称/星级/攻略徽章 + 海域血条 + 作战描述 + 出现物品 + 分支索敌/驱逐要求（含当前值满足提示）+ 道中/BOSS 掉落 + 出击按钮
- 默认选中首个未攻略海域的未攻略地图；油弹不足二次确认与一键补给保留
- 新增免登录预览页 `_preview_sortie.html`（含演示进度，供 UI 调试）
- 测试：浏览器 E2E 64 项全过，无 JS 报错；DOM 结构抽查（页签/标记位置/选中与已攻略态/详情信息行）全部正确

---

## 2026-08-06 — 备份与发布：快照 20260806_v2 + GitHub 推送

**完成：**
- 归档快照 `archive/USNavyCollection_20260806_v2/`：本次起包含完整 `public/js/data/`（旧快照 20260804/20260806 存在漏拷该目录的历史问题，本次起修正），已提交并推送
- `VERSION_HISTORY.md` 版本表新增快照行 + 当前版变更记录（海域扩充/出击UI重构章节）
- 提交：`a9e180a`（chore 归档快照）/ `18da9bb`（feat 海域扩充+出击UI重构），已推送 `origin/main`（3650150..18da9bb）

---

## 2026-08-07 — 海域拓展至 5 大区域（25 张，含 BOSS 海域，参照 kcwiki）

**问题：** 上一轮扩到 12 张后仍缺「区域最终 BOSS」的纵深；用户要求 5 个大区域 ×（4 普通图 + 1 更难的 BOSS 海域）。

**完成：**
- **25 张海域**（`data/maps.js`），星级 3★→15★：5 个区域各 4 普通图 + 1 BOSS 海域
  - **BOSS 海域**（对应 wiki EO）：1-5 夏威夷近海哨戒（wiki 1-5 全反潜图，潜水Yo级flagship BOSS，**反潜点不耗弹药仅油8%**——新增节点 `cost` 覆写）、2-5 萨沃岛近海（空母Wo级flagship×2）、3-5 阿留申海域决战（**北方栖姬在道中 H 点**，軽巡Tsu级+输送旗舰 BOSS，wiki 3-5 还原）、4-5 硫磺岛近海（折钵山栖姬 230HP）、5-5 莱特湾决战（**大和栖姬** 280HP/92甲）
  - **中太平洋海域**（wiki 6-X）：4-1 马绍尔群岛近海 / 4-2 夸贾林环礁 / 4-3 塞班岛攻略 / 4-4 菲律宾海决战（全空袭）
  - **菲律宾海域**（wiki 5-X + 莱特湾战史）：5-1 莱特湾前哨 / 5-2 苏里高海峡（夜战）/ 5-3 萨马岛近海（Re级flagship）/ 5-4 恩加尼奥角（机动部队）
- **解锁机制**：BOSS 海域需先击破同区域4号图（`need` 字段；`Sortie.start` 拦截 + UI 海图🔒锁定态 + 详情锁定提示）
- 新模板：潜水Yo级/轻巡Tsu级/战列舰Re级/驱逐IV级/空母Wo旗舰/轻母Nu旗舰/输送Wa旗舰 + 栖姬 折钵山/大和
- 任务：周常 w10/w11、月常 m5/m6、一次性 o28~o34（与舰队解锁链 o26/o27 编号衔接）
- 难度梯度实测（全流程探针）：阶段B 清 1-5~3-5（98~100%）、改二Lv80 清 4-X、满级清 5-X；S 胜率 1-5 仅 2.5%（反潜 BOSS）→ 5-5 77%；3-5 长航路 BOSS 时弹药仅 10%
- 与舰队解锁迭代集成：`mapDetailPanel` 跟随所选舰队（索敌/可出击判断）；E2E 因本机 Edge 被用户会话占用无法跑，改以 node --check + 1273 项 sim 兜底
- 测试：`npm run sim` 1273 项全过（新增 BOSS 解锁/节点 cost/5 区域梯度/新栖姬/10 组冒烟）

**下一步（后续迭代候选）：**
- [ ] 活动海域（贴条/削甲/友军/多段血条）
- [ ] EO 海域月度血条重置（勋章/战果奖励）
- [ ] 音乐/音效、战斗动画增强

---

## 2026-08-07 — 地图命名修订：Wiki 名 → 美国视角真实海域名

**问题：** 用户指出部分地图名仍过度参考 wiki（1-4 近海防卫线、1-5 近海对潜警戒、2-5 冲之岛近海、3-4 北方海域全域、3-5 北方阿留申海域），与「美国领海及临近海域」的设定不符。

**完成：**
- 地图改名（`data/maps.js`，代码注释保留 wiki 原名对照）：
  - 1-4 近海防卫线 → **欧胡岛防卫线**
  - 1-5 近海对潜警戒 → **夏威夷近海哨戒**
  - 2-5 冲之岛近海 → **萨沃岛近海**
  - 3-4 北方海域全域 → **基斯卡攻略战**
  - 3-5 北方阿留申海域 → **阿留申海域决战**
- 区域改名（`ui/sortie.js` AREA_ZH/AREA_DESC + 页签）：镇守府海域→**夏威夷海域**、南西群岛海域→**所罗门群岛海域**、北方海域→**阿留申群岛海域**、中部海域→**中太平洋海域**、南方海域→**菲律宾海域**
- 描述文案去 Wiki 术语（镇守府近海/AL泊地/冲之岛近海）；任务 o29「冲之岛制空权」→「萨沃岛制空权」
- 文档同步（README/CHARACTER/VERSION_HISTORY）
- 验证：`npm run sim` 1273 项全过（地图名仅展示层，不影响引擎逻辑）

---

## 2026-08-07 — 备份与发布：快照 20260807 + GitHub 推送

**完成：**
- 归档快照 `archive/USNavyCollection_20260807/`（完整含 `public/js/data/`，含 5-5/大和栖姬/o34/AREA_SPOTS 等全部新内容），已提交并推送
- `VERSION_HISTORY.md` 版本表更新（当前版描述 + 新快照行）
- 提交：`chore`（归档快照）+ `feat`（海域拓展至 5 大区域 25 张），推送 `origin/main`
- 注：本机 Edge 被浏览器会话占用，浏览器 E2E 无法无头运行；已用 `npm run sim` 1273 项 + `node --check` + HTTP 校验兜底

---

## 2026-08-06 — 编成/工厂/近代化列表 筛选排序改造

**完成：**
- 编成界面母港舰娘列表（`homeport.js` formation）：新增舰种筛选 chips（全部+已拥有舰种，按 `SHIP_TYPE_ZH` 顺序）+ 排序（等级 高→低/低→高、入手 新→旧/旧→新，默认等级高→低）+ 数量统计；筛选/排序状态存模块级变量，切换舰队标签/离开页面不重置；空筛选结果显示「没有符合筛选条件的舰娘」
- 数据层（`state.js`）：`createShip` 新增 `obtainedAt: Date.now()`（建造/任务/掉落均经此函数）；旧存档无此字段时排序回退 uid 序号，无需迁移
- 舰娘详情页：操作栏新增「近代化改修」按钮（改修MAX 隐藏）直接打开素材选择浮窗，完成后详情刷新；原"前往工厂"提示改为消耗规则说明
- 工厂·近代化改修页（`factory.js` modernizePanel）：目标舰列表同样新增舰种筛选+排序+数量统计（独立状态 modType/modSort）；顺带修复 `.preset-recipe` 点击处理器限定 `[data-p]`，避免新筛选 chips 触发 `undefined.split` 报错
- 近代化改修素材选择浮窗（openModernize）：同样新增筛选/排序（独立状态 matType/matSort）；已选素材被筛选隐藏仍保留在合成预览
- 编成「选择舰娘编入第N舰队」浮窗（pickShipFor）：新增筛选/排序，与母港列表共享 rosterType/rosterSort；重构为 render/wire 模式
- 排序比较器重构为参数化 `shipCmp(key, dir)`，三处列表共用
- 验证：`npm run sim` 443 项全过；headless Edge + 真实存档（空位→浮窗 27 艘候选→编入成功）零报错

**下一步（后续迭代候选）：**
- [ ] 活动海域（贴条/削甲/友军/多段血条）
- [ ] 图鉴收集率、更多海域（EO 图 1-5/2-5/3-5）
- [ ] 音乐/音效、战斗动画增强
- [ ] 浏览器 E2E 脚本化（自动化运行 test_flow）

---

## 2026-08-06 — 事故记录：编成空位选舰失效 + index.html 编码损坏（详见 `../20260806_编成筛选事故报告.md`）

**故障一：点击空位无法打开选舰浮窗**
- 现象：编成界面点击空位无任何反应。
- 根因：重构 pickShipFor 为 render/wire 模式时花括号错位，`render();` 被放入 `wire()` 内部，函数从未被调用 → 浮窗永不创建且无报错；`node --check` 语法通过但逻辑缺失。
- 修复：`render();` 移回 pickShipFor 函数体；用 DOM 桩测试 + headless Edge（CDP）+ 真实存档复现并验证（空位→浮窗→编入 全流程通过）。
- 教训：重构后必须做功能级验证，语法检查不够。

**故障二：页面乱码、游戏全崩（"鈽?缇庤埌鏀惰棌"）**
- 现象：Ctrl+F5 后页面只剩乱码，游戏无法加载。
- 根因：为强制浏览器放弃旧缓存，用 PowerShell 5.1 给 index.html 加 `?v=` 版本参数；`Get-Content` 未指定 `-Encoding`（中文系统默认 GBK）把无 BOM 的 UTF-8 文件读成乱码，`Set-Content -Encoding UTF8` 写回后中文双重损坏，且损坏字节混入 `>` 字符破坏 HTML 标签结构 → 全页崩溃。
- 修复：`git checkout -- public/index.html` 从最新提交 `3650150` 恢复；**注意**该操作一并还原了工作副本中未提交的 `#sub-modal-root` 容器行（装备选择器次级浮窗所需），已手动补回；改用 UTF-8 安全方式重新追加版本参数（21 script + 1 css）；`server.js` 静态响应加 `Cache-Control: no-cache`（需重启服务器生效）。
- 验证：headless Edge + admin 真实存档——页面中文正常、33 艘舰加载、空位选舰全流程零报错；`npm run sim` 443/443 通过。
- 教训：Windows PS 5.1 读写文本必须显式指定编码；含中文文件禁用 PS 文本管道修改；`git checkout --` 前先 `git diff` 确认；改文件后做编码校验与浏览器实测。

**遗留：** 重启服务器进程使 no-cache 头生效；建议将本次改动及时提交 git（当前工作区含大量未提交修改）；建议将 headless 浏览器回归固化为自动化脚本。

---

## 2026-08-06 战斗迭代：夜战追击选择 + 演习阵型选择（参照 kcwiki「战斗」「夜战」）

**完成：**
- 夜战不再默认进行：出击昼战演出结束后弹「追击选择」弹窗（夜战突入/战斗结束，参照 wiki 战斗流程第13~14步「追击选择→夜战」）；夜战突入追加消耗弹药10%（合计30%，油弹消耗按最终结果结算）；双方任何一方昼战后无存活舰船则直接结算、不弹窗
- 引擎（`game/battle.js`）：抽出 `nightPhase`/`settle`（结算含评价/MVP/战果），`battle()` 在 `allowNight:false` 时只做昼战并返回含阵型名的昼战结果；新增 `battleNight(dayResult)` 在共享日志上追加夜战并重新结算（评价只升不降）；演出事件助手 `makeEventHelpers` 供昼夜两段共用，UI 可从昼战播放位置无缝续播夜战
- 出击流程（`game/sortie.js`）：`advance` 拆为 `prepareBattle`（昼战，含进击检查与大破进击快照）→ `continueNight`（夜战突入）→ `settleBattle`（油弹/疲劳/轰沉/经验/掉落/血条统一结算）；`advance()` 保留原签名供模拟脚本使用
- 出击 UI（`ui/sortie.js`）：`renderBattle` 新增 `splitNight`/`nightAvailable`/`doNight`/`finish` 选项；跳过演出后仍可弹出追击选择、夜战段可继续跳过
- 演习（`ui/logistics.js`）：开战前弹出玩家阵型选择（`SortieUI.openFormationSelect`），敌方固定单纵阵（wiki：演习由玩家自由选阵型）；同样支持夜战突入选择，经验在最终结算时按最终评价发放
- 测试：`npm run sim` 831 项全过；新增 `scripts/test_night_split.js` 19 项全过（昼战分段/夜战续接/共享日志/评价不劣化/演习阵型/20%与30%弹药消耗分支）
- 文档：MEMORY.md 增加 ADR 决策与进度条目

**下一步（后续迭代候选）：**
- [ ] 活动海域（贴条/削甲/友军/多段血条）
- [ ] 图鉴收集率、更多海域（EO 图 1-5/2-5/3-5）
- [ ] 音乐/音效、战斗动画增强
- [ ] 浏览器 E2E 脚本化（自动化运行 test_flow）
---

## 2026-08-06 第三第四舰队（任务解锁 + 远征/出击/编成扩展）

**完成：**
- 舰队扩展为 4 支（state.fleet 1~4 + state.fleetUnlock{3,4}，舰队1/2初始解锁），旧档自动迁移（槽位补齐、默认锁定）；新增 Game.isFleetUnlocked / unlockFleet / unlockedFleets
- 解锁任务（均不算难）：o26「第三舰队，拔锚！」完成1次远征 → 解锁第3舰队；o27「第四舰队，出航！」击破2-1 → 解锁第4舰队；reward.unlockFleet 由 claimQuest 调 Game.unlockFleet()，任务页显示解锁奖励
- 引擎守卫：未解锁舰队拒绝远征/出击；任务「编成满编舰队」「8架舰载机」改为统计全部舰队
- UI：编成4页签（锁定页签🔒+解锁提示）；远征页舰队选择器（2/3/4）；出击页舰队选择器（1~4）；补给页按解锁舰队；母港状态栏舰队解锁进度
- 测试：npm run sim 852 项全过（新增20项）；test_flow.html E2E 70 项全过（新增6项）
- 教训：测试先写错——用未解锁的舰队3去跑解锁任务远征被引擎守卫拦截，改为用第二舰队完成远征的自然流程后通过

---

## 2026-08-07 舰艇拓展至 104 艘 + 稀有度差异化（参照舰C wiki）

**完成：**
- **舰船数据 55 → 104 艘**（纯数据追加，零引擎改动，旧档无需迁移）：
  - 经用户评审名单后新增 49 艘真实历史美舰：DD +12（含 r1 沃德/鲁本·詹姆斯、r4 基林等）、DE +1（英格兰）、CL +4（克利夫兰级等）、CA +6（含 r5 阿拉斯加大型巡洋舰）、CVL +2（兰利/卡伯特）、CV +3（提康德罗加/富兰克林/邦克山）、CVB +1（中途岛，填补装甲空母空缺）、BB +7（含新泽西/威斯康星）、SS +12（瓦胡/哈德/大青花鱼/卡瓦拉/海狮等）、AV +1（柯蒂斯，填补水上机母舰空缺，新增 AV_SLOT 槽位定义）
  - 全部只使用现有 55 件装备；数值不突破现有上限；r5 按惯例带改二；每艘含改/台词
- **稀有度差异化**（参照舰C wiki：稀有度层级 + 卡片金★惯例）：
  - 舰名按稀有度着色（common.js 新增 `shipNameHtml`/`rarityStars`；`.rn-1~5`：银白/绿/蓝/紫/金+光晕）
  - 立绘 `portraitImg` 自动加金★星标（1~5 颗）+ 稀有度边框（`.portrait-r2~5`：绿/蓝/紫/金+辉光）
  - 覆盖：舰船卡片、舰娘详情标题、秘书舰名牌、建造队列、近代化/装备改修素材列表、入渠卡片、演习敌方、海域掉落表、任务奖励
- 美术：`npm run art` 生成 106 张 SVG 立绘（104 舰 + 深海通用 2）
- 测试：`npm run sim` 1273 项全过；`node --check` 全部改动文件通过
- 备份：`backup/USNavyCollection_20260807_v2/`（改动前快照）

**下一步（后续迭代候选）：**
- [ ] 活动海域（贴条/削甲/友军/多段血条）
- [ ] 图鉴收集率、更多海域
- [ ] 音乐/音效、战斗动画增强
- [ ] 浏览器 E2E 脚本化（自动化运行 test_flow）
- [ ] 新舰补充进地图掉落表（当前全部走建造池获取）

---

## 2026-08-11 装备体系大版本：扩充/仓库/稀有度/批量/消耗品/自动上锁（参照 kcwiki「装备列表」）

**完成（多轮迭代）：**
- **装备种类扩充 55→115 件**（`data/equipment.js`，参照 kcwiki 装备列表）：新增 16 个类别——舰侦/夜间舰战/夜间舰攻/喷式舰战/对潜哨戒机/大型飞行艇/两用电探/高射装置/对空弹/照明弹/增设装甲/机关部强化/爆雷投射机/航空要员/上陆用舟艇/消耗品；原有类别补约 40 件美系装备（5inch Mk.30改+GFCS、533mm五连装鱼雷、21inch舰首鱼雷管、F6F-3/XF5U、SB2C-5/F4U-4、SG后期/SC雷达、16inch Mk.7+GFCS 等）；全部接入开发池（各池份额≤50）、改修工厂（更新进化链）、★加成、类别排序；部分高等级舰默认装备升级（弗莱彻/约翰斯顿改二 533mm、企业/埃塞克斯改二 F6F-5+SB2C-5、衣阿华级改二 Mk.7+GFCS）
- **装备仓库上限（数量系统）**：150→500 格（`state.js` EQUIP_CAP_DEFAULT，旧档迁移保留任务扩容：250→600）；满仓拦截开发/建造领取/任务领取/掉落（测试模式豁免）；**后续改为仅统计闲置装备**（`equipIdleCount()`，装备在舰艇上的不占容量，建造/舰娘奖励/掉落不再被满仓误拦截）
- **装备稀有度（等级）系统**：白/绿/蓝/紫/金 配色+标签（`UI.eqNameHtml`/`eqRarityTag`，与舰船同色系），覆盖仓库/选择器/舰娘详情/改修/开发预览/任务奖励全部展示面
- **仓库批量移除**：一键选择（升级为 类别下拉×稀有度多选 条件筛选，实时可匹配数）+ 一键移除选中（预览明细与回收量、含稀有度≥3 二次确认）；「显示装备中」开关（默认隐藏，不影响一键选择排除装备中的逻辑）
- **消耗性物资**：应急修理要员（沉没前回满耐久）/战斗粮食（夜战攻击+10%）/洋上补给（油弹回满），出击结算时真实消耗（`battle.js`+`sortie.js`）；熟练见张员/照明弹 +5% 夜战CI率、防雷鼓包 -20% 雷击伤害
- **获取自动上锁**：首个舰艇（无论稀有度，`state.firstShipLocked` 标记，旧档不追溯）+ 稀有舰艇（稀有度≥4）+ 稀有装备（≥4）+ 消耗品一律上锁；**修复舰艇解体不检查 locked 的漏洞**；获取提示「（已自动上锁）」
- **测试模式修复**：豁免仓库上限/开发等级与资源门槛/开发资材消耗；10 连停止原因提示真实原因（此前写死「开发资材不足」）
- 新任务：o35~o40（4-2~5-4 主线链+解体10件）/ w12/w13 / m7，奖励新装备与仓库扩容
- 测试：`npm run sim` 1273 项全过；专项验证（仓库/消耗品 31 项、稀有度/批量 16 项、闲置计数 17 项、自动上锁 21 项、筛选 8 项）全过
- 提交：`26efb32`（装备体系扩充+仓库上限+稀有度+批量移除+消耗品）已推送 `origin/main`；本轮（筛选/闲置计数/自动上锁）随本次心跳提交推送

**下一步（后续迭代候选）：**
- [ ] 活动海域（贴条/削甲/友军/多段血条）
- [ ] 图鉴收集率、更多海域
- [ ] 音乐/音效、战斗动画增强
- [ ] 浏览器 E2E 脚本化（自动化运行 test_flow）
- [ ] 新舰补充进地图掉落表（当前全部走建造池获取）
- [ ] 工作区含 舰艇宿舍（dormitory）功能（用户侧新增），随本次推送一并入库

---

## 2026-09-07 — 音效与音乐暂缓

用户试听后仍认为音效和音乐不达标。本次撤销音频提交 `7537468`，当前版本不包含 `public/audio/`、`audio.js` 或任何音频调用点。音频后续待找到真正合适的成品素材后再单独接入。

README 已同步更新，明确当前功能状态与测试基线。

---

## 2026-09-06 — 第一梯队四项全部落地（E2E / 音效 / 掉落表 / 图鉴）

**1. E2E 自动化（零依赖）**
- `scripts/e2e.js` + `npm run test:e2e`：自动起服务器 → 系统 Edge/Chrome 无头跑
  `test_flow.html` → 解析 `#result` JSON → 失败项退出码 1（可直接接 CI）。
- 不需要 playwright，也不需要下载浏览器二进制（本机已有 Edge / Chrome）。
- 坑：必须访问 `127.0.0.1` 而非 `localhost`；产物 `.e2e-dump.html` 已加入 .gitignore。

**2. 音效与 BGM（程序化生成）**
- `scripts/generate_audio.py`：用 python `wave` 合成 12 个音效 + 3 首 BGM，
  ffmpeg 压为 mp3（音效 64k / BGM 96k，共约 1.1MB），wav 已清理。
- `public/js/core/audio.js`：Web Audio 解码缓存、多音效叠加、BGM 循环、
  音量与静音持久化（独立 localStorage 键，不进游戏存档）、首次手势解锁。
- 接入：全局点击音、战斗事件音（炮击/雷击/航空/爆炸）、结算胜利与败北音、
  建造与远征完成音；场景 BGM 母港/战斗/凯旋自动切换；顶栏音效开关。
- **同日稍后已整体移除**：试听后音质不满意（BGM 最明显，程序化合成缺少旋律记忆点）。
  删除 `public/audio/`、`public/js/core/audio.js`、`scripts/generate_audio.py`
  与全部调用点（main/common/sortie/factory/logistics），顶栏开关一并移除。
  E2E、掉落表、图鉴三项保留。重做时可从提交 `e6e285c` 取回生成脚本与音频。

**3. 掉落表补全**
- 104 艘舰此前只有 51 艘可掉落（普通 21 + BOSS 35），**53 艘只能靠建造**。
- 按「区域主题 + 稀有度梯度」补全：r1-r2 落区域 1-2，r3 落区域 2-3，
  r4 落区域 3-4，r5 落区域 5；所罗门多巡洋驱逐、阿留申多潜艇、
  中太平洋多航母、菲律宾为最终决战；BOSS 图放该区域最稀有舰。
- 结果：**104/104 全部可掉落**。

**4. 图鉴系统**
- `state.js` 新增 `library: { ships, equips }`，记录曾经获得过的 id（解体后仍保留）。
- 存档版本 v2 → v3：旧档按当前持有补登图鉴；`libraryStats/hasShip/hasEquip` 已导出。
- 新增 `public/js/ui/library.js` + 导航项「📖 图鉴」：收集率进度条、
  舰船/装备双页签、舰种与类别筛选、稀有度筛选、只看已获得；未获得显示为灰化 ???。

**测试**：引擎 1326 项全过；迁移专项 18 项全过；浏览器 E2E 88 项全过、0 JS 错误。
**备注**：`test_flow.html` 是独立页面，有自己的 script 清单，
新增 UI 模块时**必须同时改 index.html 和 test_flow.html**，否则 E2E 报 unknown screen。

---

## 2026-09-02 — 存档迁移框架版本化

**完成：**
- `state.js` 新增 `CURRENT_SAVE_VERSION = 2`，新存档同时写入 `version/saveVersion`。
- 将旧档历史补丁集中到 `migrateV1ToV2()`，新增 `SAVE_MIGRATIONS` 版本流水线。
- 新增 `normalizeSave()`，每次读档统一修复资源、舰队、解锁状态、海域进度和容器字段。
- `applySave()` 精简为迁移 → 规范化 → 初始舰队/历史装备补发 → UID 重建 → 离线资源恢复。
- 兼容早期没有可靠版本号的存档，经验迁移仍以 `expMigrated` 兜底。
- 新增 `scripts/test_save_migration.js` 与 `npm run test:migration`，覆盖旧档、损坏档、当前档和重复迁移。
- 改动前备份：`backup/USNavyCollection_20260902_before_save_migration/`。
- 测试：迁移专项 11 项全过；`npm run sim` 1273 项全过；`node --check public/js/core/state.js` 通过。

**下一步：**
- [ ] 新增系统时继续按版本迁移器扩展，不再向 `applySave()` 添加历史补丁

---

## 2026-08-10 索敌判定 + 开幕空袭动画三步走（参照 kcwiki「索敌」「航空战」）

**问题：**
1. 战斗流程缺索敌判定与演示——原实现只是 `losReq` 二值化（恒 true），没有基于水侦/舰载机/雷达索敌值的判定，也没有演出提示。
2. 开幕空袭动画与用户预期不符：轰炸时飞机从航母重复起飞、防空炮火把所有飞机打光、步骤衔接节奏不对。

**完成：**
- **索敌判定**（`game/battle.js`，参照 wiki 2-5(33)式简化）：
  - 索敌值 = Σ[装备系数×装备索敌值] + Σ√(素索敌) − ⌈HQ×0.4⌉ + 2×(6−N)；系数：舰战/舰爆/电探/探照灯 0.6、舰攻 0.8、水侦 1.2
  - 带水侦/舰载机必定触发；按敌我索敌比值判成败；无航空兵力时低概率成功；深海不会失败
  - 成功→「命中・回避力UP」+3%；失败→「対空・回避力DOWN」-5% 且无法参加航空战
  - 索敌机未归还（减少水侦搭载，全损则昼战特殊攻击失效）仅在失败时 35% 触发（wiki 字面有「成功+未归还」，产品取舍成功=安全返回——经用户确认）
  - UI：`recon` 事件 → 雷达扫描环 + 成功/失败横幅（含未归还后缀）
- **开幕空袭三步走**（`ui/sortie.js` + `style.css`）：
  - 起飞：机群起飞悬停于舰队上空（`airGroup` 注册表）
  - 防空炮火：朝悬停机群仰射，按 `totalPlanes` 实际击落比例演示坠机、至少保留 1 架
  - 俯冲轰炸：自高空纯俯冲投弹（无重复起飞）；昼战空母航空攻击回退航母起飞
  - 衔接：空袭汇总字符串改为轰炸事件后 push（`airStrike` 返回 `{strikes, line}`），防空结束立即轰炸；防空弹幕节奏/坠机时长调校（+0.5s）
- **测试**：`npm run sim` 1273 项全过；`test_night_split` 19 项全过；E2E 70 项全过；专项验证（空母 300 局索敌成功 94.7%、顺序 300/300 正确、成功时未归还 0；flak→air 150/150）
- **顺带修复**：simulate.js 远征经验断言既有缺陷（Lv1 升级扣经验破坏 15 倍数校验，改 Lv3 初始，20/20 稳定）
- 备份：`backup/USNavyCollection_20260810_recon_air/`（改动前快照）

**下一步（后续迭代候选）：**
- [ ] 活动海域（贴条/削甲/友军/多段血条）
- [ ] 图鉴收集率、更多海域
- [ ] 音乐/音效、战斗动画增强
- [ ] 浏览器 E2E 脚本化（自动化运行 test_flow）
- [ ] 新舰补充进地图掉落表（当前全部走建造池获取）

## 2026-09-07 UI 第一阶段：桌面端全局壳层重整

**背景：**
用户确认 UI 只做桌面端（移动端不适配）、立绘冻结（占位符长期保留）、音频暂缓。
第一阶段目标是壳层：顶栏分层、导航分组、统一页头条、内容宽度调整。

**改动前备份：**
- git：远程与本地均为 `7c542c9`（推送已核验）
- 本地快照：`D:/AI/WG/backup/USNavyCollection_20260907_before_ui_shell/`（526 文件与源逐一核对）

**完成：**
- **顶栏两行化**（`index.html` + `style.css`）：身份栏（logo/提督/账号/测试模式）与资源栏（油弹钢铝/资材）拆分，资源满仓橙色 `.full` 提醒
- **导航三组重排**（`common.js` refreshNav）：司令部（母港/任务/图鉴）、舰队行动（出击/编成）、后勤与生产（宿舍/工厂/入渠/补给/远征/演习）——span 分组标签，不改路由，按钮总数仍 11
- **统一页头条**（`common.js` go() 路由层注入 PAGE_HEADS）：每页自动带「标题 + 副标题」，金色左边框样式，零逐页改动
- **内容宽度**：#app 1080 → 1240px
- **语义类预留**：badge-ok/warn/bad、btn-primary（供后续页面逐步统一状态色）
- 缓存版本号刷为 `?v=20260907ui1`（index.html 24 处，BOM 完好）

**测试：**
引擎 1326 / 迁移 18 / E2E 88 全过，0 JS 错误。
调试页截图验证（Game.load + UI.go('home')）：两行顶栏、三组导航、页头条渲染正常，母港原有内容不受影响。

**踩坑记录：**
- robocopy 经 PowerShell 调用会静默失败（stdout 也不回传）——备份改用 Python shutil.copytree 走 Bash，稳定
- 服务器拒绝点开头文件（.ui-debug.html → ui-debug.html 才可访问）
- resHtml 重构时初版 c=0（无上限资源）会误判满仓，已修（c > 0 && v >= c）

**下一步：**
- [ ] P1 玩法：特殊节点（航空战/夜战/潜艇/资源/漩涡/分歧）+ 航空触接——先出设计稿
- [ ] 设计硬约束：节点海域阶梯解锁、前置舰种获取途径自动化校验（进引擎测试）、不硬死档、关卡文案三处引导（出击前/进入时/失败结算，沉浸感+编成引导并重，用户重点要求）
- [ ] P2：任务三层 + 图鉴奖励 + 改二链 + 装备改修（克制版）
- [ ] P3：历史战役模式
- [ ] UI 第二阶段：母港重排（秘书舰/第一舰队状态列表/司令部待办）

## 2026-09-07（晚二）P1 批A：速力机制 + 夜战/潜艇/漩涡节点 + 关卡文案

**依据：**《特殊节点与航空触接设计稿.md》（用户已拍板：速力修正/触接首版数值/漩涡只扣油）。

**完成：**
- **速力机制**（`ships.js` + `battle.js`）：`SLOW_CLASSES`（纽约/宾夕法尼亚/内华达/新墨西哥/田纳西/科罗拉多/兰利级，共 11 艘）+ 全局 `shipSpeed(def)`，单舰可 `def.speed` 覆盖；战斗对象（玩家/敌方两条构建路径）均携带 speed
- **潜艇点**（`mode:'sub'`）：敌全员潜水舰开幕雷击（原来仅精锐/旗舰）；新增 `subAttackMod` 速力修正（低速BB/CV·CVE 命中×1.2 伤害×1.3；DD/CL/DE ×0.7/×0.6；CA 类 ×0.9/×0.9）；`pickSubTarget` 低速大目标权重×2；**单次雷击伤害 ≤ 目标耐久上限 60%**（防进点即大破）
- **精锐潜艇模板**：`ess4`（Le级 tp68）/`ess4f`（潜水棲姬随舰 tp88）——调试发现原 ess1（tp26）打不穿 BB 装甲（伤害公式先扣装甲后乘系数），潜艇伏击点必须用高雷装潜艇，速力修正才有发挥空间；2-2 A 点 F18 改为 ess4f+ess4+edd2e，C 点新增 F20b（ess4+ess3+eca1e+edd2e）
- **夜战点**（`mode:'night'`）：battle() 早退路径 `opts.nightOnly` 跳过昼战全阶段，结果带 `forceNight`；game 层 continueNight 自动追加夜战；UI 追击选择自动夜战突入（autoNight，不再询问）
- **漩涡节点**（`type:'whirlpool'`）：只扣燃料 `min(lossBase, 10%持有)`，舰队任一舰装备电探（SLOT.RADAR）损失减半；3-1 在 B→D 之间插入 W 节点（高索敌分歧 A→C 天然绕开，低索敌必经——完全确定性，不与既有 branch 冲突）
- **关卡文案三处**（用户重点）：海域 `brief` 字段（1-4/2-2/3-1，军事简报体）+ 出击屏/地图选择页"作战简报"框；节点进入横幅归因（nodeinfo 显示夜战点/潜艇点/漩涡及应对提示）；失败结算归因（潜艇点按"无对潜舰/有对潜舰但败北"分两种提示，夜战点同）——文案表集中在 game 层与 nodeinfo，不逐海域硬编码
- **测试**：simulate.js 新增 23 项断言——速力完整性（104舰可解析/低速11艘/新锐舰不误标）、节点完整性（潜艇点含SS/漩涡lossBase/夜战位置）、3-1 图连通（既有断言覆盖新节点）、**统计断言：潜艇对低速BB队雷击伤害 6020 vs DD队 1090（5.5×，>1.3×阈值）**、单次雷击≤60%耐久、夜战节点无昼战日志且 forceNight 可正常追加夜战
- E2E 新增 5 项（speed/night-node/sub-node/whirlpool/brief）达 93 项，0 JS 错误；迁移 18 项不变
- 缓存版本号刷为 `?v=20260907p1`

**调试教训：**
- 第一版统计指标用"舰队总承伤"被沉船干扰（DD 脆皮全沉撑高伤害），改为只统计潜艇来源的雷击事件伤害后才得到真实信号——统计断言要隔离目标变量的来源
- 伤害公式是"先扣装甲再乘系数"：低雷装攻击对高装甲目标天然无效，设计"潜艇威胁低速战列"必须配套高雷装敌编成，光加系数没用

**批B（待做）：** 航空战点（无航母被动防空分支）+ 航空触接 + 2-3 节点/简报 + 剩余海域分布铺开（1-5/2-4/2-5/3-2~3-5/4-x/5-x）+ 获取途径交叉校验规则1

## 2026-09-10 玩家测评五项修复

**来源：** 玩家测评意见 5 条（全部落实）。

**完成：**
1. **建造配方记忆**（`ui/factory.js`）：建造面板新增「记住本次配方」勾选（默认开，状态持久化 localStorage）；开始建造时保存油弹钢铝四项，下次打开自动填充
2. **强化后实时刷新**（`ui/homeport.js`）：近代化改修/改造/解体完成后，除刷新详情弹窗外同步重渲染底层页面（`UI.go(UI.current...)`）——宿舍列表里被吃掉的素材舰即时消失，不再需要 F5；工厂页保持调用方局部刷新（避免页签重置）
3. **远征自动循环 + 阵型固定**（`ui/logistics.js` + `ui/sortie.js`）：
   - 远征面板新增「自动循环」勾选（按舰队分别记忆）；领取成功后自动再派遣同一远征，条件不满足时提示原因
   - 阵型选择弹窗高亮上次使用阵型，可勾选「固定所选阵型」；固定后进击按钮直接使用（按钮文案显示固定阵型），旁有「取消固定」按钮
4. **后勤页签排序**（`ui/logistics.js`）：页内页签从「远征/入渠修理/补给/演习」改为「入渠修理/补给/远征/演习」（与顶部导航一致）；默认页从远征改为入渠
5. **结算按钮跟随流式输出**（`ui/sortie.js`）：战斗结果行与「继续前进/返回母港」按钮追加后自动滚动到底（`logEl.scrollTop = scrollHeight`），不再需要手动滚动滚轮

**测试：** 引擎 1349 / 迁移 18 / E2E 93，全部通过，0 JS 错误。缓存版本号刷为 `?v=20260910fix1`。

**提交：** `26650b1`（本地）。推送时 GitHub 443 完全不可达（直连 4 次重试均失败，症状同 09-07），**待网络恢复后补推并用 ls-remote 核验**。

**环境备忘：** 本日 Bash 会话 PATH 损坏（coreutils 全丢，grep/tail/cd 均不可用），PowerShell 输出不回传——工作流改为 PowerShell 执行 + 输出重定向到文件 + Read 工具读取。

## 2026-09-08 — 游戏策划与制作人开发规范建立

**完成：**
- 新增工作区规范文档：`D:/AI/WG/美舰收藏_游戏策划与制作人开发规范.md`
- 将原版《舰队Collection》的玩家体验研究沉淀为长期开发依据，明确设计北极星：让玩家为自己的决策承担代价，不为游戏没有说明的规则承担代价。
- 建立策划与制作人双重评审、P0/P1/P2原则、设计卡模板、Gate 0-5开发闸门、关卡文案/数值/UI规范、A级至D级制作优先级、五层质量验收标准、禁止事项和决策记录格式。
- 固化当前项目执行口径：桌面端优先；活动海域、音频、完整立绘暂缓；强制舰种必须前置可得；特殊节点不制造硬死档；随机必须可解释；关卡文案同时承担沉浸和编成教学；新系统必须配套测试、失败归因、备份和流水记录。

**后续执行：**
- 后续新增系统先填写设计卡，经策划/制作人双重评审后再进入 Gate 0-5。
- 当前开发优先继续执行规范中的 P1 批B：航空战点、航空触接、2-3 节点与简报，并同步补齐获取途径交叉校验。

## 2026-09-11 秘书舰分层立绘（近似 Live2D）实装

**背景：** 承接 09-10 的立绘方向调研。纯分割式分层（自制颜色分割 / MobileSAM / Qwen-Image-Layered）**全部失败**，根因一致：只切不补，头一转脖子就裂。改用 **See-through**（SIGGRAPH 2026，动漫角色专用，含遮挡区 inpaint 补画）——Docker + ComfyUI 路线跑通，单张立绘产出 **23 层语义图层**（前后发 / 脸 / 眼白 / 虹膜 / 睫毛 / 眉毛 / 耳 / 脖子 / 上衣 / 下装 / 手套 / 靴…，左右部件独立成层）。

**完成：**
- **资源**：`public/art/live2d/fletcher/`（23 层 PNG，**共 0.61 MB**）+ `public/js/data/live2d.js`（层坐标数据，由 `ArtPipeline/install_live2d.py` 生成）
- **驱动**：`public/js/ui/secretary-live2d.js` → `window.SecretaryL2D`（`has` / `html` / `bind`）
- **动作**：呼吸（身体组以脚底为支点纵向微缩放，**不是整体浮动**——遵守 09-10 定的红线）、头部常驻摇摆、鼠标视线跟随、**眨眼**、说话口型（点击秘书舰触发）
- **接入**（`ui/homeport.js` 三处）：`home()` 渲染优先走分层；`bindSecretaryPoke()` 守卫改为 `.sec-portrait` 或 `.l2d-wrap` 都启用；渲染后调用 `SecretaryL2D.bind()`
- **回退链**：分层立绘 → 单张 AI 立绘 → 舰种占位符（某艘船没做分层也不开天窗）
- **眨眼素材**：`install_live2d.py` 额外生成 `eyelid.png`（眼球形状 + 额头皮肤色的贴片，96×40），眨眼时 `scaleY` 0→1 从上往下盖

**两个必须知道的坑：**
1. **`neck` 层必须排在 `topwear` 之后**。See-through 的 `depth_median` 排序把衣领排在脖子前面（衣领确实更靠前），照抄原序 → 脖子被衣领完全盖住、看起来"没有脖子"。实装时我漏了这个修正（demo 里有、生成游戏数据时没跟上），已修。
2. **眼睑/嘴这类「既要跟随头部、又要自己缩放」的层，必须一层 div 包一层 img**：外层跟 `--ht`，内层做 `scaleY`。**一个元素只有一个 `transform-origin`**，旋转支点（脖子根部）和缩放支点（眼睛顶部）位置不同，混用必错。

**已知限制：** `mouth` 层实测只有 **22×6 px / 19 个不透明像素**，放大后显示约 10×3 px，口型**只能看出微弱起伏**。要做真正的口型需另做「张嘴」差分图替换 `mouth.png`。

**顺手修复：** `.gitignore` 里的 `data/` 会连 `public/js/data/`（正式源码目录）一起忽略，导致新增的 `live2d.js` 无法入库。改为 `/data/` + `archive/*/public/js/data/`。

**验证：** `node --check` 全通过；服务器资源全 200；颈部逐点对照（合成 vs 原图）色差 < 6。

**工具沉淀（在 `../ArtPipeline/`）：** 分层质量三件套 —— `compare_composite.py`（0° 全图校验）、`who_visible.py`（逐像素定位谁在遮挡）、`render_rot.py`（转动接缝，离线渲染不用开浏览器）；加 `install_live2d.py`（换舰娘只需跑它）。

## 2026-09-11 下一步方向研究（双 agent：提案 + 独立评审）

**产出：** `../design/方向提案.md`（5 个方向，均不含新船/新装备/新海域）+ `../design/方向提案_评审.md`（独立评审，含逐条代码核实）

**评审价值：** 核实提案的 12 条现状断言 —— 8 条成立、2 条有实质偏差、2 条不成立。
引用的 6 个行号 6/6 精确，但**机制解读出错两处**：
- 「交战形态是四分随机」→ 实为加权 45/30/15/10（`battle.js:796`），T 不利仅 10%，据此论证 P0-1 冲突不成立
- 「演习恢复士气 +30」→ `progression.js::applyBattleResult` 无任何 morale 写入，该机制**不存在**

评审排序（总分）：情报室 78 > 舰历 72 > 士气 70 > 侦察航向 62 > 海域目标 58，
与提案排序的分歧在于把「海域作战目标」从第 4 位调到末位（核心选择不成立 + 红线风险最高 + 与舰历冗余）。
评审建议：**只做方向 1，且砍到三件事**（制空/对潜/速力出击前展示 + 特殊攻击可触发清单 + 归因扩到制空/索敌），
去掉 25 图威胁剖面这个数据工程与那条 200 局模拟断言。

**顺手修正（重要）：** `MEMORY.md` 的士气条目长期失真，是提案事实错误的来源之一，已按代码改正 ——
旧记「出击 -30 / 回港 +15 / 命中回避 ±10~20%」，
实际「出击 -15（`sortie.js:241`）/ 回港静置每 tick +3 至 49、49~52 直接跳 53（`state.js:156-157`）/
闪 命中×1.2 回避×1.8、红脸 命中×0.5（无回避惩罚）（`battle.js:222-233`）；演习不改士气」。

**未决：** 采纳方向待定。本轮**未提交**（等方向拍板后随实现一起提交），当前工作区有 `MEMORY.md` 与 `HEARTBEAT.md` 两处未提交改动。


## 2026-09-11 开发任务书 · 批次1：方向一「出击前情报室」

**来源：** `../design/开发任务书.md` 批次1（含设计卡 `../design/设计卡_批次1_出击前情报室.md`）。四个批次按序推进，本条目只覆盖批次1。

### 改动清单

| 文件 | 改了什么 |
|---|---|
| `public/js/game/battle.js` | ① 抽出 `attackProfile()` + `DAY_SPECIALS`/`NIGHT_SPECIALS` 两张表，`resolveDayAttack`/`resolveNightAttack` 改为查表（**行为逐位不变**）；② 新增公共聚合 `playerShipsOf`/`fleetStats`/`enemyAirPower`/`hasAirSuperiority`/`specialAttackReport`；③ 导出 `buildCombatShip`/`airPower`；④ 结算结果挂 `recon`/`myAir`/`enAir`/`airSup`/`engagement`（夜战追加后由 `battleNight` 重新挂载） |
| `public/js/core/state.js` | 新增并导出 `fleetAir` / `fleetAsw` / `fleetNight` / `fleetSpeed` / `battleFleetStats`，全部**委托** `battle.js` 的 `fleetStats`（存档实例→战斗对象→聚合），UI 不得另写一套 |
| `public/js/data/maps.js` | 1-2 / 1-4 / 2-2 / 3-1 新增 `threat[]`（维度 key）与 `threatNote`（军事简报体威胁说明） |
| `public/js/game/sortie.js` | ① 新增 `intel()` / `threatCheck()` / `requiredLos()` / `fleetHasRadar()` / `fleetHasAswShip()` / `THREAT_INFO`；② 失败归因抽成**纯函数** `attributionLines()` 并扩展「制空不足」「索敌失败」两条 |
| `public/js/ui/sortie.js` | 海域详情 `md-rows` 内新增「编成自检」区块（舰队能力 / 威胁对位 / 特殊攻击清单 / 威胁评估），只提示不拦截 |
| `public/css/style.css` | `.md-rows .ok`、`.md-threat`、`.md-intel-sep`、`.md-special` |
| `scripts/simulate.js` | 新增 39 项断言；**修复 2 项既有失败断言**（弗莱彻改造需 Lv.40＝累计 78,000 经验，用例只喂了 50,000） |
| `public/test_flow.html` | 新增 14 项 E2E；**补 2 个缺失的 `<script>`**（`data/live2d.js` / `ui/secretary-live2d.js`）——09-11 分层立绘实装后 E2E 一直因 `SecretaryL2D is not defined` 整轮失败，属既有回归 |
| `public/js/ui/homeport.js` | `SecretaryL2D` 加存在性守卫（分层驱动缺失时静默降级，不整页报错） |
| `scripts/drift_check.js` + `scripts/battle_digest.baseline.txt` | **新增**战斗引擎逐位对拍工具与基线（固定种子 LCG → 9 组场景 → 评价串/伤害/日志指纹） |
| `public/index.html` | 缓存版本号统一刷为 `?v=20260911intel1` |

### 关键设计取舍

- **同源优先**：能力值、威胁判定、特殊攻击可发动清单全部由引擎算，UI 只排版着色（对齐规范 P2-2、禁止事项 6）。`fleetAir()` 与实战日志「我军制空」同值；`fleetStats.los` 与既有 `fleetLos` 同值。
- **维度规则可推导**：`对潜⟺有 sub 节点`、`夜战火力⟺有 night 节点`、`电探·燃料⟺有 whirlpool`、`索敌⟺有 los 分支`、`制空⟺某战斗点敌军有舰载机`。首版只声明 4 图，**不做 25 图数据工程**；断言做成「已声明图的维度集与推导集双向相等」+「任何已声明图不得漏必需维度」，防日后改海域时剖面漂移。
- **不拦截出击**（P0-2）：面板仅提示，按钮不禁用；未声明维度的海域整块不显示。
- **失败归因纯函数化**：`attributionLines()` 不碰 DOM，可用合成 result 直接断言，避免"为了测归因去构造真实败局"。

### 测试

| 层 | 结果 |
|---|---|
| `npm run sim` | **1386 项通过 / 0 失败**（本批 +39，含修好的 2 项既有失败） |
| `npm run test:migration` | 18 项通过（本批无存档结构改动） |
| `npm run test:e2e` | **106 项通过 / 0 JS 错误**（本批 +14；修好既有的整轮失败） |
| 引擎逐位对拍 | `node scripts/drift_check.js --against scripts/battle_digest.baseline.txt` → **9 组场景摘要逐位一致**（780+ 场战斗，含日志指纹），证明重构零数值漂移 |

### 浏览器实测（Gate 4）

1440px 截图 3 张（存档于 `../backup/2026-09-11_批次1/实测截图/`）：
- `intel_3-1.png`：3-1 显示 ✓索敌 / ✓制空 / ✗电探·燃料（含红色原因文案）；昼战特殊攻击整组折叠为「全部无法发动 —— 制空不足」；夜战 ✓夜战连击（列出舰名）
- `intel_2-2.png`：2-2 对潜 ✗（"对潜 4：该图有潜艇伏击点，建议编入驱逐舰或轻巡洋舰"）；缺穿甲弹/缺电探逐项标注
- `intel_1-1.png`：未声明维度的海域**不显示**威胁对位与威胁评估，只显示舰队能力与特殊攻击

长文案（威胁评估/AI 名称）在 350px 窄列内正常折行，无撑破、无重叠；控制台 0 错误。

### 已知问题 / 未纳入范围

- 威胁维度只填 4 张图（任务书明确要求），其余 21 张留待后续数据工程。
- 「对潜」对位只判"舰队是否含 DD/CL/DE/AS"，未做数值门槛（任务书要求只给维度、不给达标数值）。
- `daily/HEARTBEAT` 中此前记录的 E2E 93 项已被 09-11 立绘改动破坏，本批顺手修复，现为 106 项。
- 战斗引擎的"统计输出逐位相同"用**固定种子对拍工具**实现：`Math.random` 未播种时逐位相同不可复现，故本批新增 `scripts/drift_check.js` 作为此后改战斗代码的标准验证手段（批次2的方向五必用）。

## 2026-09-11 开发任务书 · 批次2：方向三「士气可见化」+ 方向五「侦察引导航向」

**来源：** `../design/开发任务书.md` 批次2。方向五先做纸面验证（Gate 1），结论见 `../design/方向五_纸面验证结论.md`。

### 方向三：士气可见化

**前提修正（任务书附录 A）**：原提案说的「演习恢复士气 +30」**不存在**——`Progression.applyBattleResult` 全文无 morale 写入。
本批改为把**现成机制显性化**：`state.js:156-157` 早就实现了「回港静置每 tick +3 至 49，49~52 直接跳到 53」，
即「歇一会儿就自动到闪」，只是玩家完全看不见。

**档位设计（4 档，边界与断言一致）**：闪 ≥50（命中 ×1.2 / 回避 ×1.8）/ 正常 40–49 / **偏低 30–39（无战斗惩罚，是预警档：
再打一场 −15 就跌入红脸）** / 红脸 <30（命中 ×0.5，无回避惩罚）。
> 任务书原文只给了 3 个区间（≥50 / 30–49 / <30）却要求「四档」，且验收边界值列了 0/29/30/49/50/100。
> 采用「40–49 为正常、30–39 为偏低」这一读法：6 个边界值全部有意义，且「偏低」有真实战术含义（一场之隔）。

| 改动 | 说明 |
|---|---|
| `battle.js` | 新增 `MORALE_TIERS` 表 + `moraleTier/moraleMods/moraleBadge`；`hitChance` 的命中与回避修正改为读该表（**行为逐位不变**） |
| `state.js` | `moraleTier/moraleMods/moraleBadge` 薄包装（UI 不硬编码 1.2/1.8/0.5） |
| `game/sortie.js` | `fleetMorale(fleetIdx)` 摘要 + `moraleAdvice(fleetIdx)` 轮换提醒；`start()` 回传 `advice`；败局归因新增「红脸命中减半」行 |
| `ui/common.js` | `stateBadges` 用档位徽记（**文字带修正数值**，不靠颜色）；闪/偏低/红脸各有独立样式 |
| `ui/sortie.js` | 编成自检面板置顶「⚠ 轮换提醒」+「舰队士气：平均 39（闪 1 ｜ 正常 1 ｜ 偏低 1 ｜ 红脸 1）｜ 修正值由档位表给出」；出击中点血条栏新增士气档位；点出击时把提醒并入 toast |
| `ui/homeport.js` | 编成页头「士气 平均X（闪/偏低/红脸）」+ 建议横幅；母港司令部状态新增「🔥 第一舰队士气」 |
| `ui/dormitory.js` | 宿舍统计 chips 从「闪」一项扩为「闪 / 士气偏低 / 红脸」三项（档位判定走 `Game.moraleTier`） |
| `css` | `.state-badge.morale-flash/low/red`、`.md-morale.red/low`、`.morale-row` |

### 方向五：侦察引导航向（先验证、后实现）

**纸面验证（任务 2.4，`scripts/recon_eng_paper.js`）**：
- 抽取段：T 不利频率实测 9.80% → 4.70%（理论差 5.00 个百分点）
- 估计段（重要性再加权，4000 场/格）：|ΔS| 最大 **3.43 个百分点**（中编队 Lv40 × 1-3 BOSS），3-1 BOSS +1.09；
  强编队/低配编队 ≈ 0 —— **感知窗口正好落在「中等强度编队 × 高难图」**，即玩家真正需要准备手段的场景
- → **≥1 个百分点，通过门槛，进入任务 2.5**

**实现（任务 2.5）**：`ENG_WEIGHTS = { base:45/30/15/10, recon:45/30/20/5 }` + `engagementWeights(reconOk, hasReconPlane)`；
触发只依赖两个玩家可见条件（**索敌成功** 且 **携带舰侦** `cat==='舰侦'`）；**不消灭 T 不利**（仍 5%）、单级、无失败惩罚；
命中引导时战报追加「（舰侦侦察引导：已提前确认敌舰队航向，T字不利概率由 10% 降至 5%）」；
出击前情报室新增「航向侦察」行（带/不带舰侦 + 概率 + 占用舰战槽的代价）。

**⚠️ 实测段给出的最重要结论（写进结论文档）**：带舰侦**不是无脑更强**，而是真取舍——
中编队 Lv40 × 1-3 BOSS：S 率 62.00% → **73.25%（+11.25pp）**；
中编队 Lv40 × 3-1 BOSS：93.03% → **87.17%（−5.85pp）**——3-1 的 D 点有敌空母，换掉舰战 → 制空掉档 → 丧失昼战特殊攻击，代价盖过航向收益。
信息侧已把两侧同时摊给玩家（制空值 + 特殊攻击清单 + 航向侦察行），玩家可自行判断该图要不要带。

### 测试

| 层 | 结果 |
|---|---|
| `npm run sim` | **1413 项通过 / 0 失败**（本批 +27；连跑 4 次稳定，统计类断言已按 4000 样本把阈值定在 ~7σ） |
| `npm run test:migration` | 18 项通过（本批无存档结构改动） |
| `npm run test:e2e` | **123 项通过 / 0 JS 错误**（本批 +17） |
| 引擎逐位对拍 | 士气重构 + 航向权重改动后，`--against battle_digest.baseline.txt` **仍是 9 组逐位一致**（无舰侦编队行为完全不变） |

### 浏览器实测（Gate 4）

1440px 截图 3 张（`../backup/2026-09-11_批次1/实测截图_批次2/`）：
- `b2_formation.png`：编成页头「士气 平均39（闪 1 / 偏低 1 / 红脸 1）」+ 橙色建议横幅 + 卡片徽记「红脸·命中×0.5」「偏低」「闪·命中×1.2/回避×1.8」
- `b2_sortie.png`：出击面板「⚠ 轮换提醒」置顶 + 士气行 + 「航向侦察：未携带舰侦…」+ 特殊攻击清单，出击按钮**未禁用**
- `b2_home.png`：母港秘书舰徽记「红脸·命中×0.5」+ 第一舰队卡片档位徽记 + 司令部状态「第一舰队士气 39（闪 1 / 偏低 1 / 红脸 1）」

### 顺手修复

- `test_flow.html` 的 1-1 攻略循环由「固定 3 轮」改为「最多 6 轮」：评价为 C/D 时不减血条，
  3 轮不保证够，**此前偶发假失败**（本轮就撞到一次 `FAIL sortie:1-1cleared`）。属既有脆弱断言。
- 新增 `scripts/recon_eng_paper.js`（方向五验证脚本，不接入 `npm run sim`）。

### 已知问题 / 未纳入范围

- 「偏低」档只做展示与预警，**没有战斗惩罚**——这是刻意的（避免凭空加一层惩罚），但它因此也不是一个"要解决的问题"，
  而是一个"下一步会变糟"的提示。
- 全库只有 1 件舰侦（`sbdvs2`，开发·空母系，占舰战槽，无分级），所以本方向的取舍空间有限，如实记录。
- 士气归因只在**参战舰里真的有红脸**时输出；红脸阈值取 <30（与战斗修正同源），而防沉保护的「红脸僚舰扣至 1」用的是 <20，
  两者是不同机制，本批**未动**（避免改战斗行为）。

## 2026-09-11 开发任务书 · 批次3：方向二「舰历」

**来源：** `../design/开发任务书.md` 批次3。**核心修正（任务书附录 A）**：原提案的「带满级旗舰求稳 vs 带 Fletcher 去拿斩杀记录」若两者收益都只是"记录"，
那是零代价的伪选择。本批按修正做：**履历记录实际战果，包含失败与大破** —— 让"用弱船出战"真的变成有代价的选择。

### 存档结构（v3 → v4）

`state.ships[uid].record`（`Game.defaultRecord()` 单点定义）：
`{ sorties, expeditions, sWin, taiha, failures, perfect, bossKills, lastBoss, firstClear{mapId:ts}, honors[{id,at}], remodelAt[] }`
- 迁移器 `migrateV3ToV4` 走 `SAVE_MIGRATIONS`，旧档每舰补默认结构（`normalizeRecord` 补齐残缺形状，不覆盖已有数值）
- **刻意不在 `normalizeSave` 里补 record**：补了就等于绕过迁移器，"迁移器坏了但测试仍绿"的假通过
- `sorties` 口径 = **每次战斗结算 +1**（与既有提督「总出击」统计一致；1-1 一次跑图两个战斗节点 = +2），已在代码注释与用例里写明

### 写入点（唯一入口 `Progression.recordBattleResult`）

| 路径 | 位置 | 用例 |
|---|---|---|
| 普通出击（含夜战追加后的二次结算，仍只写一次） | `game/sortie.js::settleBattle` | sorties/sWin/taiha/failures/firstClear/荣誉 |
| 演习 | `game/progression.js::applyBattleResult`（**仅 isPractice**，出击由 settleBattle 写，避免双计） | sorties +1 |
| 远征 | `game/logistics.js::claimExpedition` | expeditions +1（不加 sorties） |

### 荣誉（8 个，首版）

`初阵 / 初捷 / 完全胜利 / 斩首 / 夜战无双 / 反潜先锋 / 制空权确保 / MVP 之誉`
- 全部指向**可用不同打法达成的作战事实**（编夜战舰队 / 带对潜 / 带舰战夺制空 / 突破 5 舰以上编成击沉旗舰），无「累计出击 N 次」式磨时间条件
- **只做展示与排序，不含任何数值加成字段**（断言：荣誉表无 `bonus/mod/stat` 键；授予全部荣誉后 `shipStats`/`fleetStats` 逐字段不变）
- 幂等：`grantHonors` 拒绝重复 id，也拒绝表外 id

### 展示

- 舰娘详情弹窗新增「**舰历**」页签：履历摘要（8 格）+ 荣誉墙（8 格，金色=已获得）+ 真实舰史
- 战报自动追加：`本场 MVP：X（获得经验 ×2）` / `斩杀：击沉敌方旗舰「X」` / `新获得荣誉：A、B（舰名）`
- **舰史 42 艘**：覆盖 1-x/2-x/3-x 全部 BOSS 掉落舰（正好约 40 艘），新增 `ships[].bio` 字段，复用 `line` 里已埋的真史线索
- 与「图鉴」的分工写进注释：图鉴记"有没有收集到"，舰历记"我和她一起打过什么"，**不复述收集进度**（3.5 约束）

### 测试

| 层 | 结果 |
|---|---|
| `npm run sim` | **1441 项通过 / 0 失败**（本批 +28；连跑 4 次稳定） |
| `npm run test:migration` | **27 项通过**（本批 +9：v3→v4 补 record、形状一致性、残缺 record 补齐、幂等） |
| `npm run test:e2e` | **133 项通过 / 0 JS 错误**（本批 +10） |
| 引擎逐位对拍 | 仍 9 组逐位一致 |

### 顺手修复（两个都是既有隐患）

1. **批次1 留下的偶发脆弱断言**：`fleetAir 与实战「我军制空」同源` 在**索敌失败**的场次会拿不到该日志行（索敌失败改打"无法参加航空战"），失败率约 5%。
   已改为"重试到索敌成功再比对"。这是本轮实测暴露的真实假失败源。
2. **`washington` 台词史实错误**：`line` 写的是"美国海军'海军上将'级"，华盛顿号实为**北卡罗来纳级二号舰**。已改为「击沉雾岛的夜战名手！北卡罗来纳级二号舰，请求出击！」——
   与新写的 `bio` 一致（舰史不矛盾断言会卡这条）。
3. `test_flow.html` 的 E2E 不再为测弹窗而 `UI.go('home')`：**每次进母港都会启动一套秘书舰 rAF 循环**，叠加后会让 headless 的虚拟时间预算吃紧（本次就撞到 `#result 仍为 RUNNING`），改为直接 `Homeport.openShipDetail()`。

### 浏览器实测（Gate 4）

`../backup/2026-09-11_批次1/实测截图_批次3/b3_record.png`：1440px 下舰历页签完整渲染 ——
出击 12 / S 胜 9 / 大破 2 / 失败 0 / 完全胜利 8 / 击沉旗舰 5 / 通关海域 1；荣誉墙 8 格（6 已获得金色 + 2 未获得灰）；舰史正常；无 undefined、无破版。

### 已知问题 / 未纳入范围

- 舰史只写 42 艘（任务书要求的"约 40 艘"），其余 62 艘仍回退到 `line` 台词文本。
- 「斩首」只认 BOSS 节点或 5 舰以上敌方编成（避免"打沉一艘驱逐就叫斩首"），非 BOSS 的 3 舰编成不计。
- 荣誉全部是舰队级（`kind: 'fleet'`）+ 一个 MVP 个人级；没有做"某一艘单独取得的战绩"这类更细的归属（需要更细的击杀归属管道）。

## 2026-09-11 开发任务书 · 批次4：方向四「海域作战目标」

**来源：** `../design/开发任务书.md` 批次4。**风险最高、评审建议砍掉、负责人决定保留**的方向 —— 因此按修正做：
目标必须**改变编成**，不做"只判定 + 零惩罚 + 一次性奖励"那套（那套组合下最优策略是"顺手打"，等于给已有行为贴标签）。

### 存档（v4 → v5）

`record.objectives = { objId: 首次达成时间戳 }` —— **复用批次3 的 record 结构**，未另起一套。
迁移器 `migrateV4ToV5` 只补键；`normalizeRecord` 改为**按 defaultRecord 规范键序重建**（这样迁移路径与新建路径的 JSON 形状逐字符一致，迁移测试才对得上）。

### 目标设计（6 张图 / 12 个目标，全项目 ≤15）

| 图 | 目标 | 类型 | 迫使玩家做什么 |
|---|---|---|---|
| 1-2 | 编成含 ≥1 艘正规空母 | typeLimit | 必须带航母争制空（B 点敌编成有空母） |
| 1-2 | 以 S 胜击破 BOSS | sRank | 必须在输出上做足 |
| 1-4 | 编成含 ≥3 艘驱逐舰/轻巡洋舰 | typeLimit | 夜战节点无昼战 → 必须编夜战核心 |
| 1-4 | 全程无舰大破 | noHeavy | **与上一条方向相反**（轻快舰更脆），形成真实取舍 |
| 2-2 | 编成含 ≥3 艘驱逐舰 | typeLimit | 对潜 + 与「驱逐≥3 走 B 点水线」的分支条件同源 |
| 2-2 | 以 S 胜击破 BOSS | sRank | |
| 2-4 | 编成含 ≥2 艘空母 | typeLimit | 制空决战要两个空母位（牺牲两个水面输出） |
| 2-4 | 以 S 胜击破 BOSS | sRank | |
| 3-1 | 编成含 ≥2 艘战列舰 | typeLimit | **与「该图要制空」直接冲突**（战列舰挤掉空母位） |
| 3-1 | 全程无舰大破 | noHeavy | 大船更怕被围殴 |
| 3-2 | 编成含 ≥4 艘驱逐舰 | typeLimit | 该图「驱逐带路」，4 驱逐意味着放弃重火力 |
| 3-2 | 全程无舰大破 | noHeavy | |

**刻意不做**：`noNight`（不进入夜战 → 玩家点一下"战斗结束"就行，纯操作）、全清奖励、任何数值加成。

### 判定与奖励

- `Sortie.checkObjectives(map, ctx)` 是**纯函数**（不写状态、不改战斗逻辑），只在 **BOSS 节点**判定
- `noHeavy` 的"全程"口径由 `sortie.daPoSeen` 汇总（道中出现过大破 → BOSS 战达成无效），不是只看最后一场
- `sRank` 只在 BOSS 判定（道中 S 胜不算）；`typeLimit` 按舰队**实际**舰种构成判定
- 奖励：`Progression.grantObjectiveRewards` 是唯一副作用点，全局账本 `st.stats.objectives` 保证**一次性**（重复达成不再发）
- 战报追加：`作战目标「条件」：达成/未达成（一次性奖励 …）`

### 展示

- **海图详情**：目标列表 + **可核算条件**（"编成含 ≥2 艘战列舰"）+ 实时 ✗/✓ 当前值 + 一次性奖励 + 本图战功 x/y + 「战斗中达成」标记
- **图鉴**：新增「战功」区块（全部海域达成记录 x/12）

### 测试

| 层 | 结果 |
|---|---|
| `npm run sim` | **1466 项通过 / 0 失败**（本批 +25） |
| `npm run test:migration` | **32 项通过**（本批 +5：v4→v5 补 objectives、不覆盖既有履历、形状一致、幂等） |
| `npm run test:e2e` | **145 项通过 / 0 JS 错误**（本批 +12） |
| 引擎逐位对拍 | 仍 9 组逐位一致 |

**本批最关键的一条断言（任务书 4.1 断言 3）**：用**固定种子**跑同一条 1-1 全流程三次（无目标 / 有目标但未达成 / 有目标且达成），
逐项对拍 油弹·提督经验·掉落·血条·每舰消耗·每舰耐久·履历 ——
"未达成"与"无目标"**完全逐字节相同**；"达成"仅多出一次资源奖励，其余不变。这条把"目标绝不影响主结算"钉死了。

### 4.4 红线自检（逐条回答）

1. **这个目标是否迫使玩家改变编成？** —— 是。12 个目标里 8 个是 `typeLimit`（改动舰种构成才能达成），
   4 个是 `noHeavy`/`sRank`（改变防御与输出取向）。**没有任何一个目标可以靠"多点一下按钮"达成**；
   唯一被任务书点名禁止的 `noNight` 型目标一个都没有。
2. **全项目目标总数是否 ≤15？** —— 是，共 **12** 个（6 张图 × 2）。有断言守着（`作战目标总数 ≤15`）。
3. **是否存在"为了拿奖励而重复刷同一张图"的最优解？** —— **不存在**：奖励**一次性**（重复达成不再发，有断言守），
   且单次奖励量级只有 300~600 资源（远低于同图一次完整周回的正常收支），刷图本身不产生额外收益。
   四个 `noHeavy` 目标恰好是"越刷越难"（连续出击会掉士气 → 更容易被打出大破），天然反向激励搬砖。

### 浏览器实测（Gate 4）

`../backup/2026-09-11_批次1/实测截图_批次4/`：
- `b4_obj.png`：3-1 海域详情显示「作战目标（可选，只判定、不改战斗；本图战功 0/2）」+ 两条目标
  （`✗ 当前 1 艘` / `战斗中达成`）+ 各自一次性奖励 + 出击按钮未禁用
- `b4_war.png` + DOM 核对：图鉴底部「战功（海域作战目标达成记录，2/12）」+ ★1-2 两条已达成

### 已知问题 / 未纳入范围

- 目标只配 6 张图（任务书要求 6–8），1-1/1-3/1-5/2-1/2-3/2-5/3-3/3-4/3-5 与 4-x/5-x 未配。
- `sRank` 类目标对"编成"的强制力弱于 `typeLimit`（任务书把它列为可用类型，我保留了但只配在 3 张图）。
- `noHeavy` 的"全程"依赖 `sortie.daPoSeen`，它随 `state.sortie` 一起进存档；中途刷新页面后继续打完，口径仍然正确（这是刻意的）。

## 2026-09-12 海域选择页布局修复（用户反馈 UI bug）

**反馈：** 选 2-1 正常，选 2-2 时右侧详情文案变长 → 左侧地图被纵向拉长变形；另要求「攻略提示」不要放在右列里，改到地图下方新建一个 box。

### 根因

`.sortie-mapview { display:flex; align-items: stretch }` + `.area-map { flex:1.35; min-height:420px }`（无固定高度）
→ 左列地图的高度由**右列内容高度**决定。右列文案越长，地图越高；节点是按百分比定位的，于是被纵向拉伸。
用户给的 2-1 vs 2-2 差异就是这么来的（实测：旧版 1-1 地图 674×810、2-2 659×892，宽高比 0.83 / 0.74，已经变成竖图）。

### 改动

| 文件 | 改了什么 |
|---|---|
| `public/css/style.css` | ① `.sortie-mapview` 改 `align-items:flex-start`（两列各自定高）；② 新增 `.sortie-mapside`（左列容器：地图 + 简报框）；③ `.area-map` 改 `flex:0 0 auto; width:100%; aspect-ratio:5/4; min-height:420px; max-height:640px` —— 高度只由自身宽度决定；④ 新增 `.sortie-brief`（地图下方的独立简报框，沿用金色提示框视觉） |
| `public/js/ui/sortie.js` | ① 新增 `briefBox(m)`；② `mapDetailPanel` 里删掉 `.map-brief`（不再放右列）；③ 海域选择页把 `areaMapPanel` + `briefBox` 一起放进 `.sortie-mapside` |
| `public/test_flow.html` | ① 新增 `<link>` 引 style.css（**此前 E2E 页面根本没加载样式**，任何布局断言都是空转）；② 新增 8 条布局断言 |
| `scripts/e2e.js` | 无头浏览器固定 `--window-size=1080,1400`（默认 800×600 会让宽高比断言退化到 min-height 分支；1080 也正是用户报 bug 的窗口宽度） |
| `public/index.html` | 缓存版本号刷为 `20260912b5`（CSS 也改了，必须一起刷） |

### 新增的 8 条布局断言（E2E）

`layout:mapNotStretched`（1-1 与 2-2 的地图宽高比必须相等）/ `layout:mapAspectRatio`（≈5/4）/
`layout:detailRightOfMap`（右列确实在地图右侧，没换行）/ `layout:columnsTopAligned` /
`layout:briefBelowMap`（简报在 `.sortie-mapside` 里）/ `layout:briefNotInDetailColumn`（不在 `.map-detail` 内）/
`layout:briefAfterMap`（简报 top ≥ 地图 bottom）/ `layout:noBriefBoxWhenMapHasNone`（1-1 无简报不渲染空框）。

**做了反向验证（负向对照）**：临时把旧 CSS 注入回来（`align-items:stretch` + `aspect-ratio:auto; flex:1 1 auto`），
E2E 立刻报 `FAIL layout:mapNotStretched :: 1-1 674x810 / 2-2 659x892` 与 `FAIL layout:mapAspectRatio :: aspect=0.738`，
确认断言真的能抓住这个 bug，不是「怎么改都绿」。验证完已把注入删掉（`grep TEMP_NEGATIVE_CONTROL` 为 0）。

### 顺手修复：E2E 1-1 攻略循环的供给枯竭（**既有缺陷，与本批无关但会卡住验证**）

`sortie:1-1cleared` 偶发挂 10 项（`sortie:start/battles/bossFought/1-1cleared`）。
根因不是随机评价，而是**油弹**：1-1 是 S→A(战斗)→B(BOSS) 两个战斗点 = 每轮消耗油弹各 40%，
而血条 3 格需要 3 次击破 → 三轮正好消耗 120%，第 4 轮必被「舰队中有舰娘补给为零」拦下。
只要任何一轮打出 C/D（不减血条），就必然失败（约 4 成的概率）。
此前把循环从 3 轮放宽到 6 轮只是延后了失败，没解决枯竭。
**修法**：每轮出击前 `Logistics.supplyFleet(1)` + 补满耐久 —— 这正是玩家回港补给后再出击的真实流程。
修完连跑 3 次均 153 项全过。

### 测试

| 层 | 结果 |
|---|---|
| `npm run sim` | 1466 项通过 / 0 失败 |
| `npm run test:migration` | 32 项通过 |
| `npm run test:e2e` | **153 项通过 / 0 JS 错误**（本批 +8 布局断言；连跑 3 次稳定） |
| 引擎逐位对拍 | 9 组场景逐位一致（本批未碰战斗代码） |

### 浏览器实测

`../backup/2026-09-12_布局修复/`（1080px）：`final_2-1.png`、`final_2-2.png`、`final_3-1.png` ——
三张图里地图都是 674×539（5:4，完全一致），右列文案长度各不相同（2-1 最短、3-1 最长）但地图毫无变化；
2-2 与 3-1 的作战简报显示在地图下方的独立框里，1-1（无简报）不渲染空框。

### 已知问题 / 未纳入范围

- 地图固定比例后，左列（地图+简报）通常比右列短，左下会留出空白。这是「不拉伸」的必然代价；
  若希望填满，可改为让简报框吃掉剩余高度，但会让简报框高度随海域变化，反而更乱。当前选择保留空白。
- 只改了海域选择页；出击中的节点页（`sortieActive`）仍把作战简报放在面板内联位置，那里是整宽布局，不存在拉伸问题。

---

## 2026-09-12 V0.302 批次1 — 航空战点（mode:'air'）+ 被动防空分支

**依据**：`../特殊节点与航空触接设计稿.md` §2.2 air / §5.2 节点横幅 / §5.3 失败归因；
`../design/开发任务书_V0.302.md` 批次1（任务 1.1–1.3，坑 #10~#15）。

### 交付内容

| 文件 | 改了什么 |
|---|---|
| `public/js/data/maps.js` | 2-3 A 点新增 `mode:'air'`；F22 改造为「圣克鲁斯机动部队」（Wo 级 + Nu 级精锐，制空 130）；2-3 加 `brief` / `threat:['los','air']` / `threatNote`；海域定义头部注释补 mode 语义 |
| `public/js/game/battle.js` | ① 新增「航空战力」判据 `hasAirWing`（空母系 CV/CVL/CVB + 搭载格 size>0 且 plane，与引擎 markLaunch 同源）；② `opts.airMode` 触发「被动防空」分支；③ `airStrike` 增加 `capRatio`（被动防空单次轰炸 ≤ 目标耐久上限 60%，与潜艇点同口径）；④ `fleetStats` 增 `carriers/carrierNames/airWing`；⑤ `attachBattleContext` 增 `airWing/airPassive/touch`；导出 `hasAirWing/isCarrierType/PASSIVE_AA_CAP` |
| `public/js/game/sortie.js` | `prepareBattle` 传 `airMode`；`attributionLines` 增 air 分支（三种边界文案可区分）；新增 `NODE_BANNER` 文案表 + `nodeBanner()/usedNodeModes()/mapHasAirNode()`；`threatCheck` 的 air 维度改用 `airWing`；`intel()` 增 `air` 区块 |
| `public/js/ui/sortie.js` | 新节点进点横幅 `.map-banner`；`NODE_MODE_ZH` 增 air；`nodeMeta` 给 mode 节点专属图标（✈ / ☾ / ⌇）+ 图例 |
| `public/css/style.css` | `.map-banner` + `.map-node.mode-air / mode-night / mode-sub` |
| `public/index.html` | `?v=20260912b5` → `20260912c1` |
| `scripts/simulate.js` | 新增 40 条断言；威胁维度 ANNOTATED 增 2-3（4→5 张），新增「航空战点敌军须有航空战力」数据护栏 |
| `public/test_flow.html` | 新增 12 条 E2E 断言（真实 DOM 的进点横幅三种边界 + 节点图标 + 简报） |
| `public/_shot_air.html` / `scripts/shot.js` | 新增（人工核对页 + 无头浏览器截图工具，Gate 4 复用） |

### 三种「无航母」边界（坑 #12，本次最容易出错的地方）

| 情形 | 判据 | 行为 | 横幅 / 归因文案 |
|---|---|---|---|
| ① 完全没有航母 | `airWing=false` 且无 CV/CVL/CVB | 被动防空 | 「舰队没有航空母舰。全舰队，对空战斗配置——」/「失去制空权。敌机轰击毫无遮蔽的舰队。（编入航空母舰并搭载舰战可夺取制空）」 |
| ② 有航母但未搭载舰载机（空槽） | `airWing=false` 且有 CV | 被动防空 | 「航空母舰未搭载舰载机。全舰队，对空战斗配置——」/「航空母舰未搭载舰载机，无法展开航空战……」 |
| ③ 有航母但没带舰战（只带舰攻，制空 0） | `airWing=true` | **正常**航空战（只是打不赢） | 「桅顶瞭望：机群临空。这是航母之间的战斗。」 |

**判据与引擎 `markLaunch`（放飞机）同源** —— 不新造一套「谁有航母」的算法（禁止事项 6）。
**伤害封顶 60%**（`Battle.PASSIVE_AA_CAP`，与潜艇点封顶同口径）：150 场实测单次轰炸 max(dmg/hpMax) 见断言输出，不做硬死档（P0-2）。
**坑 #13 的结论**：「无航母 + 索敌失败」进航空战点只挨一次（同一分支，不叠加惩罚）。

### 测试

| 层 | 结果 |
|---|---|
| `npm run sim` | **1506 项通过 / 0 失败**（1466 → +40，连跑 3 次稳定） |
| `npm run test:migration` | 32 项通过（本批无存档字段变更） |
| `npm run test:e2e` | **165 项通过 / 0 JS 错误**（153 → +12） |
| `drift_check`（改前 / 改后） | **9 组场景逐位一致（零漂移）** —— 被动防空分支只在 `opts.airMode` 下生效，非航空战点路径一位未动 |

### 浏览器实测（Gate 4，1440px）

`../backup/2026-09-12_V0302_航空线/实测截图/`：
- `b1_air_noCV.png`：2-3 A 点无航母 → 横幅「舰队没有航空母舰。全舰队，对空战斗配置——」+ 提示「敌机将直接轰炸，仅有对空炮火还击（单次伤害封顶 60%）」
- `b1_air_withCV.png`：带舰战航母（制空 241 > 敌军 130）→ 横幅「桅顶瞭望：机群临空。这是航母之间的战斗。」
- `b1_brief_2-3.png`：海域选择页作战简报 + 威胁对位「✗ 制空：…该图含航空战点，将只能以对空炮火被动迎击敌机轰炸」；地图 5:4 未被右列文案拉伸

### 已知问题 / 未纳入范围

- 炮击战里敌方空母的「航空攻击」不适用被动防空的 60% 封顶（那是既有机制、所有战斗都有，非本分支新增）；封顶只作用于**空袭阶段**。
- 2-3 的 desc/brief 按设计稿改为「圣克鲁斯 · 航母对决」叙事（name 本就是「圣克鲁斯海域」）；原 desc 提到的「战列舰精锐」改由 BOSS 编成（F26 含 ebb2e）承接。
- 「被动防空」不额外扣资源、不额外加疲劳 —— 与普通战斗点结算完全一致。

**下一步**：批次2 航空触接（`_touchHit` 独立字段 × `_reconHit`，绝不覆盖）。

---

## 2026-09-12 V0.302 批次2 — 航空触接（数值已确认）

**依据**：设计稿 §4（触接机制，数值用户已确认）+ 任务书 V0.302 批次2（任务 2.1–2.3，**坑 #10**）。

### 交付内容

| 文件 | 改了什么 |
|---|---|
| `public/js/game/battle.js` | ① 新增触接常量 `TOUCH_MAX=0.85 / TOUCH_MY_HIT=1.15 / TOUCH_EN_RATE=0.20 / TOUCH_EN_HIT=1.10` 与 `TOUCH_AIR_BONUS{SURE:.20,SUP:.10,PAR:0,INF:0,LOST:null}`；② `isTouchPlane/touchPlaneValue/touchRate/touchReport`；③ 抽出 **`hitMods(atk)`**（索敌 × 触接）供 `hitChance` 使用并供断言检查；④ 航空战阶段之后插入触接判定（`opts.touch !== false` 才执行，且不消耗随机数）；⑤ 事件 `kind:'touch'`；⑥ `_touchHit` 的清除放进 `nightPhase` 开头（夜战不使用昼间触接，且「只打昼战」的结果仍可断言） |
| `public/js/game/sortie.js` | `intel()` 增 `touch: Battle.touchReport(fleetIdx)`（与战斗判定同源） |
| `public/js/ui/sortie.js` | `touchAnim()` 演出（侦察机盘旋圈 + 「触接成功」横幅；敌方触接红色警示）+ `playEvent`/`evDur` 分派；编成自检面板新增「航空触接」行 |
| `public/css/style.css` | `.recon-banner.touch-ok / .touch-en` + `.touch-ring`（红色警示带脉冲） |
| `scripts/drift_check.js` | 新增 `--no-touch`（等价 `USNC_NO_TOUCH=1`） |
| `scripts/battle_digest.baseline_v0301.txt` | **新增**：V0.301 的对拍基线（从此可随时验证「除触接外一位未动」） |
| `package.json` | `npm run drift` / `npm run drift:archive` / `npm run shot` |
| `scripts/simulate.js` | 新增 32 条断言（批次2 三节） |
| `public/test_flow.html` | 新增 8 条 E2E 断言（触接率阶梯 / 情报室行 / 事件顺序 / 倍率 / 横幅 CSS） |
| `public/index.html` | `?v=20260912c1` → `20260912c2` |

### 数值与公式（用户已确认的首版）

- 触发时机：**昼战航空战阶段结束后、炮击战开始前**
- 参与方：搭载**舰攻 / 水侦（水爆同槽）/ 舰侦**的舰；**舰战、舰爆不参与**（设计稿只写「舰攻或侦察机」）
- 成功率 = **min(85%, Σ√(机载值) × 5% + 制空加成)**；确保 +20% / 优势 +10% / 均势·劣势 +0 / **丧失不可触接**
  - 本作舰攻没有索敌面板（`stat.los` 为空），按设计稿「舰攻/侦察机索敌值」的语义取 `stat.los || stat.avg`（缺省用攻击力代理，量级 4~9，与舰侦/水侦索敌值同档）
  - **一架触接机都没有 → 成功率 0**（制空加成不是「无飞机也能触接」的许可）
- 成功效果：我方炮击/雷击命中 **×1.15**；敌方对称：固定 20%，敌命中 ×1.10
- 消耗：无；不做：触接等级分档 / 机种细分 / 失败惩罚

### 坑 #10 的处理（本批最关键的一处）

**没有覆盖 `_reconHit`，而是新增独立字段 `_touchHit`，在 `hitChance` 内相乘：**

| 组合 | 实际命中倍率 |
|---|---|
| 索敌成功 + 触接成功 | 1.03 × 1.15 = **1.1845（+18.45%）** |
| 仅索敌成功 | 1.03 |
| 仅触接成功（索敌失败） | 1.15 |
| 都没有 | 1.00（`× 1` 在 IEEE754 下精确，逐位不变） |

断言直接检查实测场次里同一舰同时持有 `_reconHit=1.03` 与 `_touchHit=1.15`，并验证 `hitMods().total === 1.1845`。

### 测试

| 层 | 结果 |
|---|---|
| `npm run sim` | **1538 项通过 / 0 失败**（1506 → +32，连跑 3 次稳定） |
| `npm run test:migration` | 32 项通过（无存档字段变更） |
| `npm run test:e2e` | **173 项通过 / 0 JS 错误**（165 → +8） |
| `npm run drift`（V0.302 基线） | 9 组逐位一致 |
| `npm run drift:archive`（关掉触接 vs **V0.301 基线**） | **9 组逐位一致** —— 硬证明「除触接外一位未动」 |
| 触接开启 vs V0.301 | F05 / F09 / F18 / F35 / slowBB-F18 / dd-F20b **6 组有差异（预期内）**，starter-F01 / F02 / F13-night 3 组仍逐位一致 |

**为什么会有 6 组差异（逐条说明，不是「动了不该动的」）**：
触接是 V0.302 **唯一新增的随机数消费者**。`drift_check` 的 9 组场景在同一个进程里顺序执行、共用一条 LCG 流，
因此凡是「本场发生了航空战」的场景都会多消耗随机数，其后所有场景的流位置随之后移：
- `F01` / `F02`（前两组，尚未消耗）→ 逐位一致 ✓
- `F05` / `F09` / `F18` / `F35`（本场有航空战，strong 编队触接率已达 85% 上限）→ 差异来自触接判定本身
- `slowBB-F18` / `dd-F20b`（本场无航空战，但排在触接场景之后）→ 流位置位移
- `F13-night`（nightOnly，**不经过航空战、且整场不消耗随机数**）→ 逐位一致 ✓
**关键证据是不变的那一项**：`npm run drift:archive` 把触接阶段整个关掉后 9 组全部逐位回到 V0.301 ——
说明除了触接，其余逻辑（含批次1 的被动防空）一位未动。

### 浏览器实测（Gate 4，1440px）

`../backup/2026-09-12_V0302_航空线/实测截图/b2_touch_intel.png`：
出击前情报室新增「航空触接」行 ——
「搭载触接机 5 组（TBF复仇者舰攻、SBD VS-2舰侦、SOC海鸥水侦、OS2U翠鸟水侦）——制空确保 85% ｜ 优势 73% ｜ 均势 63% ｜ 制空丧失不可触接；成功则炮击/雷击命中 ×1.15」。

### 已知问题 / 未纳入范围

- 「触接成功」的**战场演出**（侦察机盘旋圈 + 横幅）只在真实战斗回放里出现，无头截图未覆盖；E2E 断言覆盖到「事件被 push 进日志 + 顺序 + 横幅 CSS 类存在」。
- 敌方触接只要求「敌方有可出动的舰载机」，不区分机种（设计稿未细分，且敌方模板无装备概念）。
- 触接不消耗搭载数、不影响索敌机未归还判定（克制版，设计稿「不做」）。
- `TOUCH_AIR_BONUS.LOST = null`（不可触接）是**唯一**不可触接的档位；劣势（INF）按 +0 处理（设计稿只写「丧失不可触接」）。

**下一步**：批次3 获取途径交叉校验（设计稿 §3 四条规则进引擎 + 负向验证）+ 节点分布铺开。
